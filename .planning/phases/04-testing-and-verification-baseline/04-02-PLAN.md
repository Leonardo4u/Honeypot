---
phase: 04-testing-and-verification-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - data/database.py
  - scheduler.py
  - tests/test_database_integration.py
  - tests/test_scheduler_dry_run.py
autonomous: true
requirements:
  - TEST-02
  - TEST-03
must_haves:
  truths:
    - "Signal/result database writes are validated by integration-style tests"
    - "A dry-run smoke command exercises end-to-end flow without Telegram sends"
    - "Dry-run exits with clear status that can be used in verification routines"
  artifacts:
    - path: "tests/test_database_integration.py"
      provides: "Integration checks for inserir_sinal and atualizar_resultado on isolated DB"
    - path: "scheduler.py"
      provides: "Dry-run execution path and command entrypoint"
    - path: "tests/test_scheduler_dry_run.py"
      provides: "Automated smoke coverage for no-Telegram dry-run behavior"
    - path: "data/database.py"
      provides: "Schema-compatible setup for repeatable integration tests"
  key_links:
    - from: "tests/test_database_integration.py"
      to: "data/database.py"
      via: "temporary DB path + write/read assertions"
      pattern: "inserir_sinal|atualizar_resultado|sinal_existe"
    - from: "tests/test_scheduler_dry_run.py"
      to: "scheduler.py"
      via: "dry-run entrypoint with send disabled"
      pattern: "dry-run|dry_run|send_message"
---

<objective>
Add integration checks for persistence paths and implement a safe dry-run smoke command.

Purpose: Close TEST-02 and TEST-03 by verifying DB writes and enabling no-send end-to-end validation.
Output: Integration test suite for DB writes plus CLI dry-run path in scheduler.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@data/database.py
@scheduler.py
@main.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add isolated DB integration tests for signal/result write paths</name>
  <files>tests/test_database_integration.py, data/database.py</files>
  <behavior>
    - Test 1: inserir_sinal persists a row and returns a valid sinal_id.
    - Test 2: atualizar_resultado updates status/resultado/lucro_unidades for saved sinal.
    - Test 3: signal existence checks remain consistent before and after updates.
  </behavior>
  <action>Create `tests/test_database_integration.py` using temporary DB setup/teardown to avoid touching production data. If needed, adjust `data/database.py` schema bootstrap so a fresh DB contains every column required by `inserir_sinal` and result updates (including message_id_vip/message_id_free/horario). Keep changes backward-compatible and minimal, per TEST-02.</action>
  <verify>
    <automated>python -m unittest tests.test_database_integration -v</automated>
  </verify>
  <done>Signal/result persistence paths are protected by repeatable integration tests on isolated database state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and test scheduler dry-run smoke command with no Telegram side effects</name>
  <files>scheduler.py, tests/test_scheduler_dry_run.py</files>
  <behavior>
    - Test 1: Dry-run entrypoint executes one analysis cycle and exits cleanly.
    - Test 2: Dry-run does not call Telegram send operations.
    - Test 3: Dry-run still emits cycle totals/log milestones for diagnostics.
  </behavior>
  <action>Add a scheduler CLI mode (for example `python scheduler.py --dry-run-once`) that executes one safe analysis pass and exits without sending Telegram messages. Use dependency injection or a dry-run flag so logic is exercised while send operations are skipped. Create `tests/test_scheduler_dry_run.py` with mocks/stubs for external calls to keep execution deterministic and offline, satisfying TEST-03.</action>
  <verify>
    <automated>python -m unittest tests.test_scheduler_dry_run -v</automated>
  </verify>
  <done>Dry-run command provides a reliable smoke check path and guarantees no Telegram delivery during validation.</done>
</task>

</tasks>

<verification>
- [ ] python -m unittest tests.test_database_integration -v
- [ ] python -m unittest tests.test_scheduler_dry_run -v
- [ ] python scheduler.py --dry-run-once
</verification>

<success_criteria>
- TEST-02 satisfied via integration coverage of DB write paths
- TEST-03 satisfied via no-send dry-run smoke command
</success_criteria>

<output>
After completion, create `.planning/phases/04-testing-and-verification-baseline/04-02-SUMMARY.md`
</output>
