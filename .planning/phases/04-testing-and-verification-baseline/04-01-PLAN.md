---
phase: 04-testing-and-verification-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_model_analisar_jogo.py
  - tests/test_filtros_gate.py
  - tests/fixtures/jogo_base.json
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "Core model analysis returns deterministic decisions for fixed fixtures"
    - "Gate filtering emits stable reject reason codes for blocked scenarios"
    - "A full unit-test command can run model and gate checks without network access"
  artifacts:
    - path: "tests/test_model_analisar_jogo.py"
      provides: "Deterministic tests for analisar_jogo decision and output fields"
    - path: "tests/test_filtros_gate.py"
      provides: "Reason-code focused tests for triple gate rejections and pass path"
    - path: "tests/fixtures/jogo_base.json"
      provides: "Canonical fixture payload reused by model tests"
  key_links:
    - from: "tests/test_model_analisar_jogo.py"
      to: "model/analisar_jogo.py"
      via: "fixed fixture input and expected decision/assertions"
      pattern: "analisar_jogo\(|decisao|ev"
    - from: "tests/test_filtros_gate.py"
      to: "model/filtros.py"
      via: "table-driven gate assertions on reason_code"
      pattern: "aplicar_triple_gate|reason_code"
---

<objective>
Create a deterministic unit-test baseline for model scoring and gate decisions.

Purpose: Satisfy TEST-01 with executable tests that protect core signal logic from regressions.
Output: Reproducible unit tests for `analisar_jogo` and `aplicar_triple_gate`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@model/analisar_jogo.py
@model/filtros.py
@model/signal_policy.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic model tests for analisar_jogo</name>
  <files>tests/fixtures/jogo_base.json, tests/test_model_analisar_jogo.py</files>
  <behavior>
    - Test 1: Fixture with EV below threshold returns DESCARTAR and non-empty motivo.
    - Test 2: Fixture with valid edge returns expected fields (`decisao`, `edge_score`, `stake_unidades`, `rho_usado`).
    - Test 3: Output remains deterministic for same fixed input in repeated calls.
  </behavior>
  <action>Create fixture `tests/fixtures/jogo_base.json` with a stable baseline payload and write `unittest` test cases in `tests/test_model_analisar_jogo.py` covering low-EV discard and approved-decision paths. Keep tests pure and deterministic (no API calls, no Telegram). Assert concrete field presence and value invariants relevant to TEST-01.</action>
  <verify>
    <automated>python -m unittest tests.test_model_analisar_jogo -v</automated>
  </verify>
  <done>`analisar_jogo` behavior is locked by deterministic tests for both discard and approved branches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add gate reason-code unit tests for triple gate</name>
  <files>tests/test_filtros_gate.py</files>
  <behavior>
    - Test 1: Gate 1 low EV blocks with expected reason_code and bloqueado_em.
    - Test 2: Gate 2 lineup false blocks with expected reason_code.
    - Test 3: Passing payload returns aprovado=true with pass reason_code.
  </behavior>
  <action>Implement `tests/test_filtros_gate.py` with table-driven `unittest` cases for key pass/fail scenarios in `aplicar_triple_gate`. Assert stable `reason_code`, `bloqueado_em`, and `detalhes` shape so regressions in gate semantics are caught early, per TEST-01.</action>
  <verify>
    <automated>python -m unittest tests.test_filtros_gate -v</automated>
  </verify>
  <done>Core gate outcomes are covered by deterministic automated tests with stable reason-code assertions.</done>
</task>

</tasks>

<verification>
- [ ] python -m unittest tests.test_model_analisar_jogo -v
- [ ] python -m unittest tests.test_filtros_gate -v
- [ ] python -m unittest discover -s tests -p "test_*.py" -v
</verification>

<success_criteria>
- TEST-01 covered with deterministic model and gate unit tests
- Tests run locally without network/API dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/04-testing-and-verification-baseline/04-01-SUMMARY.md`
</output>
