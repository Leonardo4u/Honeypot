---
phase: 08-diagnostic-quality-evaluation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - calibrar_modelo.py
  - tests/test_calibrar_modelo_brier.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
must_haves:
  truths:
    - "Historical Brier score is reproducible when the same sample size is used"
    - "Evaluation reports fallback-to-medias usage percentage as a data-quality signal"
    - "Operator can run one command and see deterministic Brier diagnostics"
  artifacts:
    - path: "calibrar_modelo.py"
      provides: "Deterministic Brier sampling/reporting and fallback quality metrics"
    - path: "tests/test_calibrar_modelo_brier.py"
      provides: "Automated regression checks for reproducibility and fallback ratio output"
  key_links:
    - from: "calibrar_modelo.py"
      to: "model/poisson.py"
      via: "calcular_probabilidades call inside Brier loop"
      pattern: "calcular_probabilidades\(|random_state=42"
    - from: "calibrar_modelo.py"
      to: "data/xg_understat.py"
      via: "calcular_media_gols_com_xg fallback source tracking"
      pattern: "calcular_media_gols_com_xg|fonte"
---

<objective>
Harden historical Brier diagnostics so they are reproducible and explicit about xG fallback quality.

Purpose: Deliver EVAL-01 and EVAL-02 with deterministic sample behavior and auditable reporting.
Output: Stable Brier diagnostic outputs plus automated tests that fail on regressions.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/07-historical-calibration-baseline/07-01-SUMMARY.md
@.planning/phases/07-historical-calibration-baseline/07-02-SUMMARY.md
@calibrar_modelo.py
@model/poisson.py
@data/xg_understat.py
@tests/test_model_analisar_jogo.py

<interfaces>
From calibrar_modelo.py:
- calcular_brier_historico(df, n_amostras=1000)
- rodar_calibracao_completa()

From model/poisson.py:
- calcular_probabilidades(media_gols_casa, media_gols_fora, max_gols=6, liga=None, rho=None)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock deterministic Brier sampling and explicit fallback metrics (per EVAL-01/EVAL-02)</name>
  <files>calibrar_modelo.py, tests/test_calibrar_modelo_brier.py</files>
  <behavior>
    - Test 1: Running Brier diagnostics twice over the same fixture data and sample size yields the same processed-count and Brier value.
    - Test 2: When xG source fallback is used, output and returned summary expose fallback count and percentage.
  </behavior>
  <action>Refactor calcular_brier_historico to keep deterministic sampling contract explicit (fixed seed path) and return structured metrics for downstream checks while preserving operator-friendly console output. Keep fallback detection logic using fonte in {"medias", "médias"}. Do not change Poisson/EDGE business formulas.</action>
  <verify>
    <automated>python -m unittest tests.test_calibrar_modelo_brier -v</automated>
  </verify>
  <done>Brier diagnostics are deterministic and expose fallback quality metrics in a testable structure.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Preserve CLI calibration flow compatibility with new Brier diagnostic contract</name>
  <files>calibrar_modelo.py</files>
  <action>Update rodar_calibracao_completa to consume the refined Brier return shape without changing the current operator flow. Keep the same six-step execution structure and keep final recommendations text stable, only adapting references required by the new return object.</action>
  <verify>
    <automated>python calibrar_modelo.py</automated>
  </verify>
  <done>Single-command calibration still runs end-to-end and prints deterministic Brier diagnostics with fallback ratio.</done>
</task>

</tasks>

<verification>
- [ ] python -m unittest tests.test_calibrar_modelo_brier -v
- [ ] python calibrar_modelo.py
</verification>

<success_criteria>
- EVAL-01 satisfied: Brier diagnostic sampling is reproducible with fixed-seed behavior.
- EVAL-02 satisfied: fallback-to-medias usage is reported as count and percentage.
- No regression in script-first operator workflow.
</success_criteria>

<output>
After completion, create .planning/phases/08-diagnostic-quality-evaluation/08-01-SUMMARY.md
</output>
