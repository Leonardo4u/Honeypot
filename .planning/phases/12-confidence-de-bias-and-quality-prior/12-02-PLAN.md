---
phase: 12-confidence-de-bias-and-quality-prior
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - scheduler.py
  - tests/test_scheduler_quality_prior_ranking.py
  - tests/test_scheduler_dry_run.py
autonomous: true
requirements:
  - WR-06
must_haves:
  truths:
    - "Runtime candidate ranking includes explicit market+league quality prior context"
    - "Low-quality prior state is observable in gate/reject runtime logs"
    - "Dry-run and deterministic runtime checks remain stable after integration"
  artifacts:
    - path: "scheduler.py"
      provides: "Candidate ranking/filter integration with quality-prior metadata"
    - path: "tests/test_scheduler_quality_prior_ranking.py"
      provides: "Regression tests for prior-aware ranking behavior"
    - path: "tests/test_scheduler_dry_run.py"
      provides: "Compatibility checks for runtime dry-run telemetry"
  key_links:
    - from: "scheduler.py"
      to: "data/forma_recente.py"
      via: "confidence/prior context lookup before building candidate payload"
      pattern: "calcular_confianca|prior"
    - from: "scheduler.py"
      to: "model/filtros.py"
      via: "gate payload includes prior/quality context for observable decisions"
      pattern: "aplicar_triple_gate"
---

<objective>
Integrate quality-prior and de-biased confidence into runtime selection/ranking and telemetry.

Purpose: satisfy WR-06 by making candidate ordering and filtering quality-aware with explicit runtime observability.
Output: scheduler integration and regression tests proving ranking and dry-run compatibility.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-confidence-de-bias-and-quality-prior/12-RESEARCH.md
@.planning/phases/12-confidence-de-bias-and-quality-prior/12-01-PLAN.md
@scheduler.py
@model/filtros.py
@tests/test_scheduler_dry_run.py

<interfaces>
From scheduler.py:
- processar_jogos() builds candidate payload and sorts `candidatos` by score.
- Runtime telemetry uses log_event for gate decisions and cycle totals.

From model/filtros.py:
- aplicar_triple_gate(dados_sinal, sinais_hoje=...)

From data/forma_recente.py (after 12-01):
- numeric confidence compatibility function and prior/context companion contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject quality-prior context into candidate ranking and gate observability (WR-06)</name>
  <files>scheduler.py, tests/test_scheduler_quality_prior_ranking.py</files>
  <read_first>scheduler.py, model/filtros.py, tests/test_scheduler_runtime_gates.py</read_first>
  <behavior>
    - Test 1: Candidates with equivalent edge but stronger quality prior rank above weak-prior candidates.
    - Test 2: Weak prior state contributes bounded penalty/adjustment rather than hard failure unless gate rules require it.
    - Test 3: Reject/accept log payloads include prior quality metadata for traceability.
  </behavior>
  <action>Integrate prior/context output from confidence layer into scheduler candidate assembly and ranking formula. Keep ranking deterministic and bounded to avoid destabilizing selection. Preserve existing policy thresholds (`MIN_EDGE_SCORE`, `MIN_CONFIANCA`) while enriching decision context and telemetry payloads.</action>
  <acceptance_criteria>
    - scheduler.py candidate payload includes prior fields (for example `qualidade_prior`, `amostra_prior`, `ajuste_prior`).
    - ranking sort path uses prior-aware composite score or explicit bounded adjustment.
    - tests/test_scheduler_quality_prior_ranking.py validates deterministic prior-aware ordering.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_scheduler_quality_prior_ranking -v</automated>
  </verify>
  <done>Runtime ranking and gate logs are explicitly quality-aware and deterministic.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Preserve runtime compatibility and dry-run safety after prior integration</name>
  <files>scheduler.py, tests/test_scheduler_dry_run.py</files>
  <read_first>scheduler.py, tests/test_scheduler_dry_run.py</read_first>
  <action>Update or extend dry-run tests to assert compatibility of cycle execution and health telemetry with new prior/context fields. Ensure no Telegram side effects and no regression in no-send flow. Keep fallback behavior stable when prior data is sparse or unavailable.</action>
  <acceptance_criteria>
    - Existing dry-run tests continue to pass with prior integration enabled.
    - New/updated dry-run assertions validate prior-context presence without breaking current log contract.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_scheduler_dry_run -v</automated>
  </verify>
  <done>Prior-aware runtime remains operationally stable and dry-run-safe.</done>
</task>

</tasks>

<verification>
- python -m unittest tests.test_scheduler_quality_prior_ranking -v
- python -m unittest tests.test_scheduler_dry_run -v
</verification>

<success_criteria>
- WR-06 satisfied with explicit prior-aware ranking and observable quality state.
- Runtime behavior remains deterministic and compatible with existing dry-run and telemetry baseline.
</success_criteria>

<output>
After completion, create .planning/phases/12-confidence-de-bias-and-quality-prior/12-02-SUMMARY.md
</output>
