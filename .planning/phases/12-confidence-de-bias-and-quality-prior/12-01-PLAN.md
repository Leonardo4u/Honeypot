---
phase: 12-confidence-de-bias-and-quality-prior
plan: 01
type: execute
wave: 1
depends_on:
  - 11-02
files_modified:
  - data/quality_prior.py
  - data/database.py
  - data/forma_recente.py
  - tests/test_confidence_quality_prior.py
autonomous: true
requirements:
  - WR-05
must_haves:
  truths:
    - "Confidence output is no longer dominated by self-selected team history when sample is low"
    - "Confidence includes explicit quality-prior influence with minimum-sample guardrails"
    - "Confidence contract remains usable by existing runtime call sites"
  artifacts:
    - path: "data/quality_prior.py"
      provides: "Historical market+league quality prior computation and quality-state contract"
    - path: "data/forma_recente.py"
      provides: "Debiased confidence builder with component-aware output"
    - path: "data/database.py"
      provides: "Query helper(s) for quality-prior historical aggregates"
    - path: "tests/test_confidence_quality_prior.py"
      provides: "Deterministic tests for prior states and de-biased confidence behavior"
  key_links:
    - from: "data/forma_recente.py"
      to: "data/quality_prior.py"
      via: "confidence composition combines team-history signal with market+league prior"
      pattern: "quality_prior|calcular_.*prior"
    - from: "data/quality_prior.py"
      to: "data/database.py"
      via: "aggregated historical statistics query"
      pattern: "sqlite3|SELECT.*sinais"
---

<objective>
Build the confidence de-bias foundation and explicit prior contract used by runtime ranking.

Purpose: satisfy WR-05 by replacing self-referential confidence behavior with sample-aware, prior-informed confidence.
Output: debiased confidence logic, market+league prior helper, and deterministic tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-confidence-de-bias-and-quality-prior/12-RESEARCH.md
@data/database.py
@data/forma_recente.py
@scheduler.py

<interfaces>
From data/database.py:
- buscar_historico_time(time, ultimos=20)
- calcular_confianca_calibrada(time_casa, time_fora)

From data/forma_recente.py:
- calcular_confianca_dados(time_casa, time_fora)

From scheduler.py:
- processar_jogos() consumes `confianca = calcular_confianca_dados(home, away)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create quality-prior computation contract and persistence query helpers (WR-05)</name>
  <files>data/quality_prior.py, data/database.py, tests/test_confidence_quality_prior.py</files>
  <read_first>data/database.py, data/forma_recente.py, calibrar_modelo.py</read_first>
  <behavior>
    - Test 1: Prior builder returns `qualidade=sem_sinal` when no historical rows exist for liga+mercado.
    - Test 2: Prior builder returns `qualidade=baixa_amostra` below minimum sample threshold.
    - Test 3: Prior builder returns `qualidade=ok` with stable numeric prior value when threshold is met.
    - Test 4: Query helpers are deterministic for same fixture dataset and parameter set.
  </behavior>
  <action>Create a focused helper module for market+league historical prior and add minimal DB aggregation helpers needed by that module. Keep APIs simple dict-based and avoid new dependencies. Reuse existing `sinais` schema/fields (`liga`, `mercado`, `resultado`, `fonte`) and keep logic tolerant to sparse data.</action>
  <acceptance_criteria>
    - data/quality_prior.py exports a function that returns `qualidade`, `amostra`, and numeric prior contribution.
    - data/database.py contains query helper(s) used by quality-prior module and does not break existing DB callers.
    - tests/test_confidence_quality_prior.py contains deterministic cases for sem_sinal, baixa_amostra, and ok.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_confidence_quality_prior -v</automated>
  </verify>
  <done>Quality-prior contract exists with deterministic sample-aware behavior and automated coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor confidence calculation to reduce selection bias and expose provenance (WR-05)</name>
  <files>data/forma_recente.py, tests/test_confidence_quality_prior.py</files>
  <read_first>data/forma_recente.py, data/database.py, scheduler.py</read_first>
  <behavior>
    - Test 1: Low-sample team history cannot yield max confidence without supportive prior.
    - Test 2: Adequate-sample historical evidence with positive prior increases confidence deterministically.
    - Test 3: Public numeric confidence return remains compatible with existing scheduler usage.
  </behavior>
  <action>Refactor confidence composition to combine team-history evidence with the new market+league prior and minimum-sample guardrails. Expose component/provenance data through an auxiliary function or metadata path while preserving the existing numeric return path required by scheduler.</action>
  <acceptance_criteria>
    - calcular_confianca_dados still returns numeric confidence for legacy caller compatibility.
    - A companion confidence-context contract exists and includes at least `amostra`, `qualidade`, and `origem` fields.
    - Tests validate anti-bias behavior across low/medium/high sample scenarios.
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_confidence_quality_prior -v</automated>
  </verify>
  <done>Confidence logic is de-biased, sample-aware, and backward-compatible.</done>
</task>

</tasks>

<verification>
- python -m unittest tests.test_confidence_quality_prior -v
</verification>

<success_criteria>
- WR-05 satisfied with explicit, test-backed confidence de-bias behavior.
- Market+league prior contract is ready for runtime ranking integration.
</success_criteria>

<output>
After completion, create .planning/phases/12-confidence-de-bias-and-quality-prior/12-01-SUMMARY.md
</output>
