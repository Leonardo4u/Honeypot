---
phase: 14-calibration-automation-and-league-home-advantage
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - model/poisson.py
  - tests/test_poisson_over_under_dc.py
autonomous: true
requirements:
  - CAL-03
must_haves:
  truths:
    - "Mercados 1x2 e over/under usam a mesma base de correcao Dixon-Coles"
    - "Probabilidades over e under permanecem normalizadas (~1.0)"
    - "Correcoes de placares baixos impactam under de forma consistente"
  artifacts:
    - path: "model/poisson.py"
      provides: "Calculo over/under com matriz DC compartilhada"
    - path: "tests/test_poisson_over_under_dc.py"
      provides: "Regressoes de normalizacao e efeito de baixa pontuacao"
  key_links:
    - from: "model/poisson.py"
      to: "model/poisson.py"
      via: "reuso da matriz DC entre metodos"
      pattern: "tau"
---

<objective>
Aplicar consistencia Dixon-Coles no mercado over/under.

Purpose: cumprir CAL-03 e reduzir divergencia metodologica entre mercados no mesmo jogo.
Output: calculo over/under com correcao DC e testes de consistencia.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-calibration-automation-and-league-home-advantage/14-RESEARCH.md
@.planning/phases/14-calibration-automation-and-league-home-advantage/14-01-PLAN.md
@model/poisson.py

<interfaces>
From model/poisson.py:
- tau(i, j, lambda_casa, lambda_fora, rho)
- calcular_probabilidades(...)
- calcular_prob_over_under(media_gols_casa, media_gols_fora, linha=2.5, max_gols=10)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reutilizar matriz DC no calculo de over/under</name>
  <files>model/poisson.py, tests/test_poisson_over_under_dc.py</files>
  <behavior>
    - Test 1: `prob_over + prob_under` permanece ~1.0 apos correcao DC.
    - Test 2: Para cenarios com forte impacto de placares baixos, under reflete ajuste esperado vs versao raw.
    - Test 3: Parametro de liga/rho manual continua suportado sem quebrar chamadas legadas.
  </behavior>
  <action>Refatorar `calcular_prob_over_under` para usar a mesma logica de matriz corrigida por `tau` (com normalizacao) ja utilizada em `calcular_probabilidades`. Expor optional `liga`/`rho` mantendo backward compatibility para chamadas existentes.</action>
  <verify>
    <automated>python -m unittest tests.test_poisson_over_under_dc -v</automated>
  </verify>
  <done>Over/under passa a compartilhar a mesma semantica DC do mercado 1x2 com testes verdes.</done>
</task>

</tasks>

<verification>
- python -m unittest tests.test_poisson_over_under_dc -v
</verification>

<success_criteria>
- CAL-03 atendido com consistencia matematica entre mercados e regressao automatizada.
</success_criteria>

<output>
After completion, create `.planning/phases/14-calibration-automation-and-league-home-advantage/14-02-SUMMARY.md`
</output>
