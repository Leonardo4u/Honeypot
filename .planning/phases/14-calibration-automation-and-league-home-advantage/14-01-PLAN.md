---
phase: 14-calibration-automation-and-league-home-advantage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - calibrar_modelo.py
  - model/poisson.py
  - data/calibracao_ligas.json
  - tests/test_poisson_calibracao_runtime.py
autonomous: true
requirements:
  - CAL-01
  - CAL-02
must_haves:
  truths:
    - "Rho por liga e fator de casa por liga podem ser atualizados por calibracao sem edicao manual de codigo"
    - "Runtime usa calibracao persistida quando disponivel e cai para fallback estavel quando indisponivel"
    - "Poisson aplica home advantage por liga de forma configuravel"
  artifacts:
    - path: "calibrar_modelo.py"
      provides: "Persistencia automatica de calibracao por liga"
    - path: "model/poisson.py"
      provides: "Loader de calibracao e aplicacao de fator casa por liga"
    - path: "tests/test_poisson_calibracao_runtime.py"
      provides: "Regressao de fallback e prioridade de calibracao"
  key_links:
    - from: "calibrar_modelo.py"
      to: "data/calibracao_ligas.json"
      via: "saida de calibracao"
      pattern: "json.dump"
    - from: "model/poisson.py"
      to: "data/calibracao_ligas.json"
      via: "loader de calibracao em runtime"
      pattern: "calibracao_ligas"
---

<objective>
Automatizar aplicacao de calibracao por liga e incluir home-advantage league-aware no Poisson.

Purpose: cumprir CAL-01 e CAL-02 sem depender de atualizacao manual em `RHO_POR_LIGA`.
Output: calibracao persistida e consumida automaticamente no runtime com cobertura de teste.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-calibration-automation-and-league-home-advantage/14-RESEARCH.md
@calibrar_modelo.py
@model/poisson.py

<interfaces>
From model/poisson.py:
- calcular_probabilidades(media_gols_casa, media_gols_fora, max_gols=6, liga=None, rho=None)
- RHO_POR_LIGA, RHO_DEFAULT

From calibrar_modelo.py:
- calibrar_rho_por_liga(df)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persistir calibracao de ligas apos execucao do calibrador</name>
  <files>calibrar_modelo.py, data/calibracao_ligas.json, tests/test_poisson_calibracao_runtime.py</files>
  <behavior>
    - Test 1: Quando calibracao roda com ligas validas, arquivo `data/calibracao_ligas.json` e escrito com `rho` por liga.
    - Test 2: Arquivo contem metadados minimos (timestamp/temporada/amostra) para auditoria basica.
    - Test 3: Falha de escrita nao derruba execucao completa sem log explicito.
  </behavior>
  <action>Atualizar `calibrar_rho_por_liga` para salvar automaticamente saida calibrada em `data/calibracao_ligas.json` com contrato estavel por liga (`rho`, `home_advantage` opcional). Incluir seed de `home_advantage=1.0` quando ainda nao calibrado para manter interface pronta para evolucao sem quebrar consumo.</action>
  <verify>
    <automated>python -m unittest tests.test_poisson_calibracao_runtime -v</automated>
  </verify>
  <done>Calibracao deixa artefato consumivel pelo runtime sem etapa manual de copia para `RHO_POR_LIGA`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consumir calibracao em runtime e aplicar home advantage por liga</name>
  <files>model/poisson.py, tests/test_poisson_calibracao_runtime.py</files>
  <behavior>
    - Test 1: `calcular_probabilidades(..., liga=X)` prioriza `rho` do arquivo calibrado quando presente.
    - Test 2: Sem arquivo ou liga ausente, comportamento cai para `RHO_POR_LIGA`/`RHO_DEFAULT` sem regressao.
    - Test 3: `home_advantage` por liga multiplica lambda da casa somente quando diferente de 1.0.
  </behavior>
  <action>Adicionar loader resiliente de `data/calibracao_ligas.json` em `model/poisson.py` e aplicar `home_advantage` por liga no caminho de calculo DC (com fallback para 1.0). Manter assinatura publica atual para compatibilidade dos chamadores existentes.</action>
  <verify>
    <automated>python -m unittest tests.test_poisson_calibracao_runtime -v</automated>
  </verify>
  <done>Poisson fica league-aware para rho/home-advantage com fallback estavel e testes deterministas.</done>
</task>

</tasks>

<verification>
- python -m unittest tests.test_poisson_calibracao_runtime -v
</verification>

<success_criteria>
- CAL-01 e CAL-02 atendidos com calibracao automatica e uso runtime sem regressao.
</success_criteria>

<output>
After completion, create `.planning/phases/14-calibration-automation-and-league-home-advantage/14-01-SUMMARY.md`
</output>
