---
phase: 03-operational-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/database.py
  - scheduler.py
autonomous: true
requirements:
  - OPS-01
  - OPS-02
must_haves:
  truths:
    - "Repeated scheduler windows do not execute the same job twice"
    - "Per-fixture failures do not abort full processing loops"
  artifacts:
    - path: "data/database.py"
      provides: "Persistent job execution registry and idempotency helpers"
    - path: "scheduler.py"
      provides: "Guarded job wrappers and explicit degraded-cycle handling"
  key_links:
    - from: "scheduler.py"
      to: "data/database.py"
      via: "idempotency key lookup/register before job run"
      pattern: "job_execution|idempot|registrar_execucao"
    - from: "scheduler.py"
      to: "scheduler.py"
      via: "per-job guarded execution wrapper"
      pattern: "rodar_analise|rodar_verificacao|rodar_clv|rodar_steam"
---

<objective>
Implement persistent idempotency control and explicit failure-isolation guards for scheduled jobs.

Purpose: Prevent duplicate runs from overlapping windows and keep batch throughput stable under per-item errors.
Output: SQLite-backed job execution control and guarded runtime wrappers in scheduler flow.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-operational-resilience/03-CONTEXT.md
@.planning/phases/03-operational-resilience/03-RESEARCH.md
@data/database.py
@scheduler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add persistent job execution registry and idempotency helpers</name>
  <files>data/database.py</files>
  <read_first>data/database.py, scheduler.py, .planning/phases/03-operational-resilience/03-CONTEXT.md</read_first>
  <action>Add idempotency persistence in `data/database.py` by creating `job_execucoes` table with columns: `id`, `job_nome`, `janela_chave`, `status`, `started_at`, `finished_at`, `reason_code`, `detalhes_json`. Add helpers with concrete signatures: `garantir_tabela_execucoes()`, `buscar_execucao_job(job_nome, janela_chave)`, `iniciar_execucao_job(job_nome, janela_chave)`, `finalizar_execucao_job(job_nome, janela_chave, status, reason_code=None, detalhes_json=None)`. Enforce unique pair (`job_nome`, `janela_chave`) and return stable outcomes for duplicate start attempts.</action>
  <verify>
    <automated>python -m py_compile data/database.py</automated>
  </verify>
  <acceptance_criteria>
    - data/database.py contains "CREATE TABLE IF NOT EXISTS job_execucoes"
    - data/database.py contains "UNIQUE(job_nome, janela_chave)"
    - data/database.py contains "def iniciar_execucao_job(job_nome, janela_chave):"
    - data/database.py contains "def finalizar_execucao_job(job_nome, janela_chave, status"
  </acceptance_criteria>
  <done>Idempotency state can be persisted and queried per job-window key.</done>
</task>

<task type="auto">
  <name>Task 2: Wire guarded scheduler execution with duplicate skip and degraded-cycle state</name>
  <files>scheduler.py</files>
  <read_first>scheduler.py, data/database.py, .planning/phases/03-operational-resilience/03-CONTEXT.md</read_first>
  <action>In `scheduler.py`, implement guarded execution wrapper using helpers from `data/database.py`: create deterministic `janela_chave` per job using target date/hour slot, call `iniciar_execucao_job` before each `rodar_*` body, skip execution when duplicate key already finished/running, and log `reason_code=idempotent_skip`. In per-game loops (`processar_jogos`, `verificar_resultados_automatico`), keep fixture-level isolation with explicit structured error logs and set cycle status to `degraded` when critical persistence operations fail (for example failed `inserir_sinal` or failed `atualizar_resultado`). Ensure wrapper always calls `finalizar_execucao_job` with `status=ok|degraded|failed`.</action>
  <verify>
    <automated>python -m py_compile scheduler.py</automated>
  </verify>
  <acceptance_criteria>
    - scheduler.py contains "idempotent_skip"
    - scheduler.py contains call to "iniciar_execucao_job("
    - scheduler.py contains call to "finalizar_execucao_job("
    - scheduler.py contains "status='degraded'" or "status = 'degraded'"
  </acceptance_criteria>
  <done>Scheduler prevents duplicate job-window execution and surfaces degraded cycles without aborting full loops.</done>
</task>

</tasks>

<verification>
- [ ] python -m py_compile data/database.py
- [ ] python -m py_compile scheduler.py
</verification>

<success_criteria>
- OPS-01 satisfied via persistent idempotent job-window control
- OPS-02 satisfied via explicit per-fixture isolation and degraded critical-path handling
</success_criteria>

<output>
After completion, create `.planning/phases/03-operational-resilience/03-01-SUMMARY.md`
</output>
