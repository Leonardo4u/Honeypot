---
phase: 03-operational-resilience
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - scheduler.py
  - data/database.py
autonomous: true
requirements:
  - OPS-03
  - OPS-04
must_haves:
  truths:
    - "Startup fails fast when critical env/db/schema prerequisites are missing"
    - "Critical workflow milestones emit stable structured logs for diagnosis"
  artifacts:
    - path: "scheduler.py"
      provides: "Startup preflight gate and structured logging helper"
    - path: "data/database.py"
      provides: "Schema health checks used by preflight"
  key_links:
    - from: "scheduler.py"
      to: "data/database.py"
      via: "preflight schema/database validation"
      pattern: "preflight|schema|sqlite3.connect"
    - from: "scheduler.py"
      to: "scheduler.py"
      via: "structured log calls around job lifecycle"
      pattern: "log_event|categoria|reason_code"
---

<objective>
Add startup preflight fail-fast checks and structured operational logging for daily diagnosis.

Purpose: Ensure runtime starts only in healthy critical conditions and emits actionable, consistent diagnostics.
Output: Preflight gate + structured logging contract integrated in scheduler lifecycle.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-operational-resilience/03-CONTEXT.md
@.planning/phases/03-operational-resilience/03-RESEARCH.md
@.planning/phases/03-operational-resilience/03-01-PLAN.md
@data/database.py
@scheduler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement startup preflight contract for critical runtime dependencies</name>
  <files>scheduler.py, data/database.py</files>
  <read_first>scheduler.py, data/database.py, .planning/phases/03-operational-resilience/03-CONTEXT.md</read_first>
  <action>Add `executar_preflight()` in `scheduler.py` and call it at the top of `iniciar_scheduler()` before registering schedules. Validate concrete critical inputs: `BOT_TOKEN`, `CANAL_VIP`, database file path, and minimum schema availability (`sinais` plus `job_execucoes`). In `data/database.py`, add helper `validar_schema_minimo()` returning tuple `(ok, missing_items)` by checking table existence in sqlite master. When preflight critical check fails, print actionable message with missing items and abort startup via `raise SystemExit(1)`. Non-critical components (for example Excel update path) should emit warning only and continue.</action>
  <verify>
    <automated>python -m py_compile scheduler.py; python -m py_compile data/database.py</automated>
  </verify>
  <acceptance_criteria>
    - scheduler.py contains "def executar_preflight():"
    - scheduler.py contains "raise SystemExit(1)"
    - data/database.py contains "def validar_schema_minimo():"
    - scheduler.py contains "BOT_TOKEN" and "CANAL_VIP"
  </acceptance_criteria>
  <done>Scheduler startup blocks on critical readiness failures with explicit diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2: Standardize structured operational logs across scheduler milestones</name>
  <files>scheduler.py</files>
  <read_first>scheduler.py, .planning/phases/03-operational-resilience/03-CONTEXT.md, .planning/codebase/CONVENTIONS.md</read_first>
  <action>Create a lightweight `log_event(categoria, etapa, entidade, status, reason_code=None, detalhes=None)` helper in `scheduler.py` that outputs stable structured lines. Replace key milestone prints with structured calls at minimum points: scheduler boot start/end, preflight pass/fail, each guarded job start/skip/end, cycle totals, and critical persistence failures. Ensure idempotent skips and degraded cycles always include `reason_code` and contextual details so daily diagnosis can grep by category/status.</action>
  <verify>
    <automated>python -m py_compile scheduler.py</automated>
  </verify>
  <acceptance_criteria>
    - scheduler.py contains "def log_event(categoria, etapa, entidade, status"
    - scheduler.py contains "reason_code"
    - scheduler.py contains structured log calls for preflight and job lifecycle
    - scheduler.py contains "idempotent_skip" and "degraded"
  </acceptance_criteria>
  <done>Operational logs are consistent, structured, and cover required diagnosis milestones.</done>
</task>

</tasks>

<verification>
- [ ] python -m py_compile scheduler.py
- [ ] python -m py_compile data/database.py
</verification>

<success_criteria>
- OPS-04 satisfied via fail-fast preflight on critical runtime prerequisites
- OPS-03 satisfied via stable structured logs across scheduler lifecycle
</success_criteria>

<output>
After completion, create `.planning/phases/03-operational-resilience/03-02-SUMMARY.md`
</output>
