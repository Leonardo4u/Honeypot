---
phase: 03-operational-resilience
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - scheduler.py
  - data/database.py
  - iniciar_bot.bat
autonomous: true
gap_closure: true
requirements:
  - OPS-03
  - OPS-04
must_haves:
  truths:
    - "Scheduler inicia sem fechar imediatamente quando schema baseline esta ausente"
    - "Preflight continua fail-fast para falhas criticas reais de ambiente e banco"
    - "Operador consegue ler erro de startup ao executar via .bat"
  artifacts:
    - path: "scheduler.py"
      provides: "Ordem de startup corrigida para bootstrap de schema antes da validacao"
    - path: "data/database.py"
      provides: "Helper de bootstrap minimo reutilizavel pelo startup"
    - path: "iniciar_bot.bat"
      provides: "Observabilidade de erro com pausa condicional"
  key_links:
    - from: "scheduler.py"
      to: "data/database.py"
      via: "bootstrap e validacao de schema"
      pattern: "garantir_tabela_execucoes|validar_schema_minimo|executar_preflight"
    - from: "iniciar_bot.bat"
      to: "scheduler.py"
      via: "execucao com captura de exit code"
      pattern: "python scheduler.py|errorlevel|pause"
---

<objective>
Corrigir o bloqueio de startup detectado no UAT e restaurar observabilidade operacional para retestes da fase 03.

Purpose: remover falso negativo de preflight e permitir diagnostico claro em execucao via .bat.
Output: startup funcional com preflight coerente + script de inicializacao com erro visivel.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-operational-resilience/03-UAT.md
@.planning/debug/03-preflight-schema-blocking.md
@scheduler.py
@data/database.py
@iniciar_bot.bat
</context>

<tasks>

<task type="auto">
  <name>Task 1: Corrigir ordem de bootstrap e preflight no startup</name>
  <files>scheduler.py, data/database.py</files>
  <action>Garantir criacao do baseline de schema necessario para operacao (incluindo job_execucoes) antes da validacao de schema minimo de preflight. Ajustar startup para evitar falso negativo de preflight quando a tabela ainda nao foi bootstrapada no primeiro run. Manter fail-fast para falhas criticas reais (env vars e indisponibilidade de banco).</action>
  <verify>
    <automated>python -m py_compile scheduler.py; python -m py_compile data/database.py</automated>
  </verify>
  <done>Scheduler nao encerra por falta de job_execucoes no primeiro startup; preflight continua bloqueando apenas falhas criticas reais.</done>
</task>

<task type="auto">
  <name>Task 2: Melhorar observabilidade de erro no inicializador .bat</name>
  <files>iniciar_bot.bat</files>
  <action>Atualizar script de inicializacao para exibir codigo de erro e manter janela aberta em falha (pause condicional), preservando execucao simples em sucesso. Garantir que o operador consiga ler mensagens de preflight ao executar manualmente ou via startup do Windows.</action>
  <verify>
    <automated>powershell -NoProfile -Command "cmd /c iniciar_bot.bat"</automated>
  </verify>
  <done>Falhas de startup ficam visiveis no terminal, sem fechamento imediato sem contexto.</done>
</task>

</tasks>

<verification>
- [ ] python -m py_compile scheduler.py
- [ ] python -m py_compile data/database.py
- [ ] iniciar_bot.bat mantem output legivel em falha
</verification>

<success_criteria>
- Gap de startup encerrando imediatamente e removido
- Preflight permanece robusto para falhas criticas
- Rounds de UAT da fase 03 ficam destravados para reteste funcional
</success_criteria>

<output>
After completion, create `.planning/phases/03-operational-resilience/03-03-SUMMARY.md`
</output>
