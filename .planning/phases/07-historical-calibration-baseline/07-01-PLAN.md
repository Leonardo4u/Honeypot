---
phase: 07-historical-calibration-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - calibrar_modelo.py
  - data/historico/
autonomous: true
requirements:
  - CAL-01
  - CAL-03
must_haves:
  truths:
    - "Operator can run one command and the script starts a full calibration cycle"
    - "Historical CSV files are reused from cache when already present"
    - "Script does not stop entire run because of single-row or single-match failures"
  artifacts:
    - path: "calibrar_modelo.py"
      provides: "Main calibration entrypoint and cache-aware historical ingestion"
    - path: "data/historico"
      provides: "Per-league per-season CSV cache tree"
  key_links:
    - from: "calibrar_modelo.py"
      to: "data/historico"
      via: "Cache check before download"
      pattern: "os\.path\.exists\(path\)|\[CACHE\]"
    - from: "calibrar_modelo.py"
      to: "model/poisson.py"
      via: "Imports probability calibration primitives"
      pattern: "from poisson import"
---

<objective>
Create a deterministic and cache-aware historical ingestion baseline for calibration runs.

Purpose: Satisfy CAL-01 and CAL-03 by ensuring the operator has a single reliable command and no unnecessary re-downloads.
Output: A robust calibration script flow with CSV caching and normalized historical loading.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-historical-calibration-baseline/07-RESEARCH.md
@.planning/phases/07-historical-calibration-baseline/07-VALIDATION.md
@calibrar_modelo.py
@model/poisson.py
@data/xg_understat.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Harden historical download cache workflow and deterministic folder strategy</name>
  <files>calibrar_modelo.py</files>
  <read_first>calibrar_modelo.py, model/poisson.py, data/xg_understat.py</read_first>
  <action>In calibrar_modelo.py, keep and harden the cache-first logic for data/historico/{liga}/{temporada}.csv so existing files are reused without HTTP calls, and missing files are downloaded with timeout protection. Keep per-league/per-season counters and final total output. Ensure download failures print scoped errors and continue to next season or league without aborting the whole run.</action>
  <acceptance_criteria>
    - File calibrar_modelo.py contains a cache branch using os.path.exists(path) before requests.get.
    - Script output includes [CACHE] when files already exist.
    - Script output includes total games summary line.
    - Download exception path uses continue behavior (no raise in per-season loop).
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile calibrar_modelo.py</automated>
  </verify>
  <done>CSV ingestion is deterministic and cache-aware, preserving single-command reliability.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Normalize historical dataframe contract and preserve resilient row handling</name>
  <files>calibrar_modelo.py</files>
  <read_first>calibrar_modelo.py</read_first>
  <action>Ensure carregar_historico in calibrar_modelo.py enforces stable internal column names (time_casa, time_fora, gols_casa, gols_fora and optional odds/date columns), drops invalid rows, casts goals to int, and concatenates all available league-season datasets. Keep row-level and file-level failures isolated so malformed CSV slices do not stop aggregation.</action>
  <acceptance_criteria>
    - File includes explicit rename mapping from HomeTeam/AwayTeam/FTHG/FTAG to internal names.
    - Dataframe processing includes dropna subset for goals and team names.
    - gols_casa and gols_fora are cast to int.
    - Aggregation returns None when no valid dataframes are loaded.
  </acceptance_criteria>
  <verify>
    <automated>python calibrar_modelo.py</automated>
  </verify>
  <done>Script can run full load path from one command with resilient normalization and no hard-stop on isolated bad records.</done>
</task>

</tasks>

<verification>
- [ ] python -m py_compile calibrar_modelo.py
- [ ] python calibrar_modelo.py
</verification>

<success_criteria>
- CAL-01 satisfied: operator runs one command and ingestion/loading stages execute in sequence.
- CAL-03 satisfied: cache reuse occurs for previously downloaded CSV files.
- Historical loading remains resilient to partial data quality failures.
</success_criteria>

<output>
After completion, create .planning/phases/07-historical-calibration-baseline/07-01-SUMMARY.md
</output>
