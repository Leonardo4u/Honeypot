---
phase: 07-historical-calibration-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - calibrar_modelo.py
  - model/poisson.py
autonomous: true
requirements:
  - CAL-02
must_haves:
  truths:
    - "Calibration run prints rho values by league with baseline comparison"
    - "Operator can see sample size per league while evaluating calibration confidence"
    - "Run output clearly tells how to apply calibrated rho values"
  artifacts:
    - path: "calibrar_modelo.py"
      provides: "Per-league rho calibration report and operator next-step guidance"
  key_links:
    - from: "calibrar_modelo.py"
      to: "model/poisson.py"
      via: "Uses estimar_rho and compares with RHO_POR_LIGA defaults"
      pattern: "estimar_rho|RHO_POR_LIGA"
---

<objective>
Deliver league-level rho calibration output with clear operational interpretation.

Purpose: Satisfy CAL-02 by exposing current versus calibrated rho and sample-size context per league.
Output: Operator-facing calibration table and next-step guidance for updating model rho defaults.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-historical-calibration-baseline/07-RESEARCH.md
@calibrar_modelo.py
@model/poisson.py
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build per-league rho calibration report with baseline delta and sample size</name>
  <files>calibrar_modelo.py</files>
  <read_first>calibrar_modelo.py, model/poisson.py</read_first>
  <action>In calibrar_modelo.py, keep calibrar_rho_por_liga wired to poisson.estimar_rho and poisson.RHO_POR_LIGA. Ensure report prints columns for liga, rho atual, rho calibrado, delta and number of matches used. Maintain dictionary return keyed by league for downstream reuse. Keep robust handling when league datasets are present but small, relying on estimar_rho fallback behavior from model/poisson.py.</action>
  <acceptance_criteria>
    - Script output header contains Liga, Atual, Calibrado, Delta, N jogos.
    - Function imports both estimar_rho and RHO_POR_LIGA from poisson.
    - Each unique league in dataframe is iterated exactly once for calibration summary.
    - Function returns a dictionary of calibrated rho values.
  </acceptance_criteria>
  <verify>
    <automated>python calibrar_modelo.py</automated>
  </verify>
  <done>Operator receives actionable league-level calibration output tied to model baseline.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Ensure final run guidance clearly directs rho update workflow</name>
  <files>calibrar_modelo.py</files>
  <read_first>calibrar_modelo.py</read_first>
  <action>Keep rodar_calibracao_completa output with explicit post-run operator instructions telling where to update rho values and how to interpret model quality status. Preserve numbered next steps and include command reminder for rerun. Do not add automatic edits to model/poisson.py in this phase; this phase outputs guidance only.</action>
  <acceptance_criteria>
    - Final output includes a numbered PROXIMOS PASSOS section.
    - Output explicitly references updating RHO_POR_LIGA in poisson.py.
    - Output includes the rerun command python calibrar_modelo.py.
    - No write operation modifies model/poisson.py during execution.
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile calibrar_modelo.py</automated>
  </verify>
  <done>Calibration reporting is complete and operationally actionable without unintended side effects.</done>
</task>

</tasks>

<verification>
- [ ] python calibrar_modelo.py
- [ ] python -m py_compile calibrar_modelo.py
</verification>

<success_criteria>
- CAL-02 satisfied: output presents league calibration deltas with sample size context.
- Operator guidance is explicit and repeatable for applying rho updates.
- Phase stays additive and safe: no automatic mutation of model default table.
</success_criteria>

<output>
After completion, create .planning/phases/07-historical-calibration-baseline/07-02-SUMMARY.md
</output>
