---
phase: 09-historical-persistence-and-operator-safety
plan: 01
type: execute
wave: 1
depends_on:
  - 08-01
  - 08-02
files_modified:
  - data/database.py
  - calibrar_modelo.py
  - tests/test_historico_backfill.py
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
must_haves:
  truths:
    - "Historical backfill writes records tagged with fonte='historico' so downstream confidence logic can filter safely"
    - "Re-running historical backfill does not create duplicates (idempotent behavior)"
    - "Single-row failures during backfill are isolated and do not abort remaining insertions"
  artifacts:
    - path: "data/database.py"
      provides: "Schema helpers for fonte and duplicate-safety constraints"
    - path: "calibrar_modelo.py"
      provides: "Historical backfill flow with duplicate skip counters and row-level isolation"
    - path: "tests/test_historico_backfill.py"
      provides: "Automated regression coverage for HIST-01/HIST-02/HIST-03"
  key_links:
    - from: "calibrar_modelo.py"
      to: "data/database.py"
      via: "Schema readiness and duplicate guard calls before insert loop"
      pattern: "garantir_.*fonte|CREATE UNIQUE INDEX|fonte='historico'"
    - from: "calibrar_modelo.py"
      to: "data/edge_protocol.db::sinais"
      via: "INSERT with source tagging and per-row failure isolation"
      pattern: "INSERT INTO sinais|except Exception:"
---

<objective>
Harden historical backfill persistence so calibration confidence consumes clean and safe historical records.

Purpose: Complete HIST-01, HIST-02, and HIST-03 with explicit source tagging, idempotent duplicate protection, and row-level failure tolerance.
Output: Backfill-safe schema + robust insertion flow + regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/07-historical-calibration-baseline/07-01-SUMMARY.md
@.planning/phases/08-diagnostic-quality-evaluation/08-01-SUMMARY.md
@.planning/phases/08-diagnostic-quality-evaluation/08-02-SUMMARY.md
@data/database.py
@calibrar_modelo.py
@tests/test_database_integration.py

<interfaces>
From calibrar_modelo.py:
- popular_banco_historico(df, n_max=2000)
- _odd_valida(valor)

From data/database.py:
- DB_PATH
- criar_banco()
- garantir_schema_minimo()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add schema-level historical source safety and idempotent duplicate guardrails (HIST-01/HIST-02)</name>
  <files>data/database.py, tests/test_historico_backfill.py</files>
  <behavior>
    - Test 1: sinais schema always has coluna fonte with default bot when database is initialized or migrated.
    - Test 2: duplicate historical key insertion is blocked by idempotent constraint/index behavior.
  </behavior>
  <action>In data/database.py, add schema compatibility helpers to guarantee coluna fonte in sinais and enforce duplicate-safety for historical rows (prefer partial unique index for fonte='historico' over broad global uniqueness). Keep backward compatibility for existing databases and avoid blocking regular bot rows. Add tests in tests/test_historico_backfill.py using temporary DB and migration path coverage.</action>
  <verify>
    <automated>python -m unittest tests.test_historico_backfill.TestHistoricoSchemaSafety -v</automated>
  </verify>
  <done>Historical source tagging and duplicate-safety contract are guaranteed at schema layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor historical backfill insert loop for safe idempotent reruns with row isolation (HIST-02/HIST-03)</name>
  <files>calibrar_modelo.py, tests/test_historico_backfill.py</files>
  <behavior>
    - Test 1: First run inserts valid historical rows with fonte='historico'.
    - Test 2: Second run over same sample does not duplicate rows and reports skipped duplicates.
    - Test 3: Injected bad row fails without aborting remaining valid rows.
  </behavior>
  <action>Refine popular_banco_historico in calibrar_modelo.py to rely on schema safety from data/database.py and implement explicit counters (inseridos, duplicados, falhas). Keep per-row try/except continue semantics and preserve existing Poisson/EV formulas. Ensure inserts always write fonte='historico' and reruns are safe/idempotent by skipping already-existing historical keys.</action>
  <verify>
    <automated>python -m unittest tests.test_historico_backfill.TestHistoricoBackfillFlow -v</automated>
  </verify>
  <done>Backfill reruns are idempotent and resilient to partial-row failures.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify end-to-end calibration command still preserves operator-safe behavior</name>
  <files>calibrar_modelo.py</files>
  <action>Run full calibration script and confirm [6/6] backfill stage reports safe behavior (existing-historico skip or duplicate-safe summary) without crashing the command. Keep operator-facing messages concise and explicit about inserted/skipped/failed counts when applicable.</action>
  <verify>
    <automated>python calibrar_modelo.py</automated>
  </verify>
  <done>Operator can run one command and historical persistence behaves safely with no duplicate pollution.</done>
</task>

</tasks>

<verification>
- [ ] python -m unittest tests.test_historico_backfill -v
- [ ] python calibrar_modelo.py
</verification>

<success_criteria>
- HIST-01 satisfied: historical rows are source-tagged via fonte='historico'.
- HIST-02 satisfied: reruns do not create duplicates and duplicates are skipped safely.
- HIST-03 satisfied: row-level failures are isolated and batch continues.
</success_criteria>

<output>
After completion, create .planning/phases/09-historical-persistence-and-operator-safety/09-01-SUMMARY.md
</output>
