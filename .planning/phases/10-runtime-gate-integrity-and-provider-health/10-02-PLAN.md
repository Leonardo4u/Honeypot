---
phase: 10-runtime-gate-integrity-and-provider-health
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - data/coletar_odds.py
  - scheduler.py
  - tests/test_scheduler_provider_health.py
autonomous: true
requirements:
  - WR-09
must_haves:
  truths:
    - "Provider health counters classify odds-provider outcomes by category on every league request"
    - "Timeout, HTTP, connection, empty payload, and unknown statuses are not collapsed into a single bucket"
    - "Cycle telemetry keeps stable structure while adding complete status coverage"
  artifacts:
    - path: "data/coletar_odds.py"
      provides: "Odds fetch function variant returning status metadata from retry layer"
    - path: "scheduler.py"
      provides: "Provider status-to-counter mapping and cycle summary integration"
    - path: "tests/test_scheduler_provider_health.py"
      provides: "Regression tests for per-status counter accounting"
  key_links:
    - from: "data/coletar_odds.py"
      to: "data/ingestion_resilience.py"
      via: "request_with_retry status propagation"
      pattern: "request_with_retry\(|status"
    - from: "scheduler.py"
      to: "data/coletar_odds.py"
      via: "league fetch call that consumes status metadata"
      pattern: "buscar_jogos_com_odds|provider_health"
---

<objective>
Complete provider-health observability so runtime cycle telemetry reflects real ingestion failure categories.

Purpose: Complete WR-09 with deterministic status mapping and regression coverage.
Output: Status-aware odds fetch integration and validated provider health counters.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-runtime-gate-integrity-and-provider-health/10-RESEARCH.md
@.planning/phases/10-runtime-gate-integrity-and-provider-health/10-01-SUMMARY.md
@data/coletar_odds.py
@data/ingestion_resilience.py
@scheduler.py
@tests/test_scheduler_dry_run.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Propagate retry-layer status metadata from odds collection into scheduler (WR-09)</name>
  <files>data/coletar_odds.py, scheduler.py, tests/test_scheduler_provider_health.py</files>
  <behavior>
    - Test 1: `ok` responses increment only `provider_health['ok']`.
    - Test 2: `timeout`/`http_error`/`connection_error` increment their specific counters.
    - Test 3: `empty_payload` increments empty counter without misclassifying as connection error.
    - Test 4: Unknown status maps to explicit fallback bucket and remains visible in logs.
  </behavior>
  <action>Add a status-aware fetch path (new function or structured return) in data/coletar_odds.py while preserving current API compatibility for existing callers. In scheduler, consume status metadata per league request and centralize status mapping in a helper to prevent drift and duplicated conditionals.</action>
  <verify>
    <automated>python -m unittest tests.test_scheduler_provider_health -v</automated>
  </verify>
  <done>Provider health counters are complete and accurately categorized per fetch outcome.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Preserve cycle summary and log_event compatibility with enriched provider telemetry</name>
  <files>scheduler.py</files>
  <action>Ensure console health summary and `log_event` payload remain backward-compatible while including enriched status categories. Keep output concise and stable for operator monitoring and downstream JSON parsing.</action>
  <verify>
    <automated>python -m unittest tests.test_scheduler_dry_run -v</automated>
  </verify>
  <done>Operational telemetry remains compatible while giving full provider-health signal.</done>
</task>

</tasks>

<verification>
- [x] python -m unittest tests.test_scheduler_provider_health -v
- [x] python -m unittest tests.test_scheduler_dry_run -v
</verification>

<success_criteria>
- WR-09 satisfied: each ingestion outcome category updates the corresponding provider health counter.
- No regressions in dry-run flow or cycle telemetry emission.
- Health summary is complete and actionable for runtime operations.
</success_criteria>

<output>
After completion, create .planning/phases/10-runtime-gate-integrity-and-provider-health/10-02-SUMMARY.md
</output>
