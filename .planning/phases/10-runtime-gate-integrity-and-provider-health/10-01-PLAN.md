---
phase: 10-runtime-gate-integrity-and-provider-health
plan: 01
type: execute
wave: 1
depends_on:
  - 09-01
files_modified:
  - scheduler.py
  - tests/test_scheduler_runtime_gates.py
autonomous: true
requirements:
  - WR-01
  - WR-02
must_haves:
  truths:
    - "Gate 2/3/4 inputs are populated from runtime context rather than fixed placeholders"
    - "Daily limit check during candidate filtering uses real count including in-cycle sends"
    - "Gate reject reason codes are visible in runtime logs for blocked candidates"
  artifacts:
    - path: "scheduler.py"
      provides: "Runtime gate context builder and candidate-time gate invocation with real inputs"
    - path: "tests/test_scheduler_runtime_gates.py"
      provides: "Regression tests for gate context and daily-limit enforcement behavior"
  key_links:
    - from: "scheduler.py"
      to: "model/filtros.py"
      via: "aplicar_triple_gate payload with dynamic escalacao/variacao/sinais_hoje"
      pattern: "aplicar_triple_gate\(|escalacao_confirmada|variacao_odd|sinais_hoje"
---

<objective>
Restore effective runtime gate behavior so candidate approvals reflect real context and daily capacity.

Purpose: Complete WR-01 and WR-02 by removing hardcoded gate placeholders in scheduler candidate flow.
Output: Gate payload helpers + runtime integration + regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-runtime-gate-integrity-and-provider-health/10-RESEARCH.md
@scheduler.py
@model/filtros.py
@data/steam_monitor.py
@tests/test_filtros_gate.py
@tests/test_scheduler_dry_run.py

<interfaces>
From scheduler.py:
- processar_jogos(dry_run=False)
- validar_entrada_analise(jogo, odd)

From model/filtros.py:
- aplicar_triple_gate(dados_sinal, sinais_hoje=0)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce runtime gate-context helpers and remove placeholder values (WR-01)</name>
  <files>scheduler.py, tests/test_scheduler_runtime_gates.py</files>
  <behavior>
    - Test 1: Gate payload no longer sends fixed `escalacao_confirmada=True` for all candidates.
    - Test 2: Gate payload no longer sends fixed `variacao_odd=0.0` when opening snapshot context is available.
    - Test 3: Missing context paths still produce safe fallback values and do not crash candidate loop.
  </behavior>
  <action>Refactor scheduler candidate loop to compute gate inputs using explicit helper functions (lineup context and odd variation context). Keep behavior deterministic and avoid introducing external API dependency in this phase. Ensure helper outputs are traceable in candidate diagnostics for future tuning.</action>
  <verify>
    <automated>python -m unittest tests.test_scheduler_runtime_gates.TestGateContextBuilder -v</automated>
  </verify>
  <done>Gate 2 and Gate 3 inputs originate from runtime-derived context rather than constants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce real daily-limit value at gate time with in-cycle accounting (WR-02)</name>
  <files>scheduler.py, tests/test_scheduler_runtime_gates.py</files>
  <behavior>
    - Test 1: Candidate filtering passes `sinais_hoje_real` to Gate 4 instead of zero.
    - Test 2: As candidates are accepted/sent, remaining capacity updates and prevents overflow.
    - Test 3: Blocked candidates expose gate reason code in runtime logs.
  </behavior>
  <action>Update scheduler gate invocation so `sinais_hoje` reflects database count plus in-cycle accepted sends where applicable. Keep top-level max-signal cutoff and selected-candidate slicing consistent with existing design. Add reject logging with reason code to make gate decisions auditable.</action>
  <verify>
    <automated>python -m unittest tests.test_scheduler_runtime_gates.TestDailyLimitGate -v</automated>
  </verify>
  <done>Gate 4 is effective during runtime selection and reason codes are observable.</done>
</task>

</tasks>

<verification>
- [ ] python -m unittest tests.test_scheduler_runtime_gates -v
- [ ] python -m unittest tests.test_filtros_gate tests.test_scheduler_dry_run -v
</verification>

<success_criteria>
- WR-01 satisfied: Gate 2/3 runtime inputs are non-placeholder and context-driven.
- WR-02 satisfied: Gate 4 uses real daily count and blocks overflow with explicit reason visibility.
- Dry-run and existing scheduler flow remain functional.
</success_criteria>

<output>
After completion, create .planning/phases/10-runtime-gate-integrity-and-provider-health/10-01-SUMMARY.md
</output>
