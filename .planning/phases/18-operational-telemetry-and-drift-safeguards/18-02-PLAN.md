---
phase: 18-operational-telemetry-and-drift-safeguards
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - data/verificar_resultados.py
  - data/quality_telemetry.py
  - scheduler.py
  - tests/test_settlement_fixture_resolution.py
  - tests/test_scheduler_settlement_integrity.py
  - tests/test_quality_telemetry_weekly.py
autonomous: true
requirements:
  - SETTLE-01
  - OBS-02
must_haves:
  truths:
    - "Settlement usa janela de busca configuravel por perfil de competicao"
    - "Jogos europeus/copas conseguem janela maior sem afetar ligas domesticas"
    - "Drift alerta somente quando degradacao persiste em horizonte historico, nao por ruido de 1 ciclo"
  artifacts:
    - path: "data/verificar_resultados.py"
      provides: "Resolucao de janela de settlement por perfil de competicao"
    - path: "scheduler.py"
      provides: "Propagacao de contexto de competicao e envio de alerta de drift historico"
    - path: "data/quality_telemetry.py"
      provides: "Avaliacao de drift por historico rolling"
    - path: "tests/test_settlement_fixture_resolution.py"
      provides: "Regressoes da janela configuravel por competicao"
    - path: "tests/test_scheduler_settlement_integrity.py"
      provides: "Regressao scheduler->resultado com janela por perfil"
  key_links:
    - from: "scheduler.py"
      to: "data/verificar_resultados.py"
      via: "buscar_resultado_jogo(..., liga=...)"
      pattern: "janela_dias"
    - from: "scheduler.py"
      to: "data/quality_telemetry.py"
      via: "avaliacao drift historico + alerta"
      pattern: "drift"
---

<objective>
Adicionar settlement configuravel por competicao e alerta de drift com persistencia historica.

Purpose: cumprir SETTLE-01 e OBS-02 reduzindo pendencias de resultado e disparos de alerta por ruido curto.
Output: janela de settlement por perfil + drift alert rolling com integracao no scheduler.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-settlement-label-integrity/11-01-SUMMARY.md
@.planning/phases/11-settlement-label-integrity/11-02-SUMMARY.md
@.planning/phases/18-operational-telemetry-and-drift-safeguards/18-01-PLAN.md
@data/verificar_resultados.py
@scheduler.py

<interfaces>
From data/verificar_resultados.py:
- buscar_resultado_jogo(time_casa, time_fora, data=None, horario=None, fixture_id=None)
- _buscar_fixture_por_janela(..., janela_dias=2)

From scheduler.py:
- verificar_resultados_automatico()
- log_event(...)

Current contract notes:
- fallback de settlement usa janela fixa de +/-2 dias
- alerta de drift atual e minimo (fallback_rate por ciclo), sem confirmacao em historico rolling
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tornar janela de settlement configuravel por perfil de competicao</name>
  <files>data/verificar_resultados.py, scheduler.py, tests/test_settlement_fixture_resolution.py, tests/test_scheduler_settlement_integrity.py</files>
  <behavior>
    - Test 1: Competicoes europeias/copas usam janela maior que ligas domesticas por configuracao.
    - Test 2: Perfil default preserva comportamento atual quando contexto de competicao nao estiver disponivel.
    - Test 3: Scheduler propaga contexto da liga/perfil ao resolver resultado pendente.
  </behavior>
  <action>Introduzir mapa configuravel de janela de settlement por perfil/competicao e aplicar esse valor na busca por data em `buscar_resultado_jogo`. Integrar chamada no scheduler para repassar contexto de liga do sinal, mantendo caminho fixture-id-first e retrocompatibilidade dos contratos existentes.</action>
  <verify>
    <automated>python -m unittest tests.test_settlement_fixture_resolution tests.test_scheduler_settlement_integrity -v</automated>
  </verify>
  <done>SETTLE-01 atendido com janela configuravel e regressao cobrindo perfis domesticos e europeus.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar alerta de drift em horizonte historico rolling</name>
  <files>data/quality_telemetry.py, scheduler.py, tests/test_quality_telemetry_weekly.py</files>
  <behavior>
    - Test 1: Drift alerta quando metricas (Brier/WR/fallback) excedem limiares por janelas consecutivas.
    - Test 2: Sem persistencia de degradacao, alerta nao dispara (evita falso positivo de ciclo isolado).
    - Test 3: Payload de alerta inclui periodo, metrica e severidade para auditoria operacional.
  </behavior>
  <action>Adicionar avaliador de drift rolling sobre snapshots persistidos (fase 18-01), com limiares configuraveis e criterio de persistencia minima. Integrar no scheduler para log estruturado e envio de mensagem Telegram quando degradacao sustentada for detectada.</action>
  <verify>
    <automated>python -m unittest tests.test_quality_telemetry_weekly -v</automated>
  </verify>
  <done>OBS-02 atendido com alerta robusto por tendencia historica e menor sensibilidade a ruido de curto prazo.</done>
</task>

</tasks>

<verification>
- python -m unittest tests.test_settlement_fixture_resolution tests.test_scheduler_settlement_integrity -v
- python -m unittest tests.test_quality_telemetry_weekly -v
- python scripts/run_tests.py
</verification>

<success_criteria>
- SETTLE-01 e OBS-02 atendidos com janela de settlement por perfil e drift alert rolling integrado ao scheduler.
</success_criteria>

<output>
After completion, create `.planning/phases/18-operational-telemetry-and-drift-safeguards/18-02-SUMMARY.md`
</output>
