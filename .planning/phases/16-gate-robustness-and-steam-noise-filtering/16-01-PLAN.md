---
phase: 16-gate-robustness-and-steam-noise-filtering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - model/filtros.py
  - data/steam_monitor.py
  - tests/test_filtros_gate.py
  - tests/test_scheduler_runtime_gates.py
autonomous: true
requirements:
  - GATE-01
  - STEAM-01
must_haves:
  truths:
    - "Gate 5 motivation cache survives scheduler restarts while respecting TTL refresh"
    - "Steam bonus is not applied before a minimum market-open elapsed window"
    - "Steam confirmation remains bounded by consensus and opening-window constraints"
  artifacts:
    - path: "model/filtros.py"
      provides: "Persistent standings cache policy with controlled TTL"
    - path: "data/steam_monitor.py"
      provides: "Steam maturity gate requiring minimum elapsed market-open time"
    - path: "tests/test_filtros_gate.py"
      provides: "Regression for cache read/write and TTL invalidation behavior"
    - path: "tests/test_scheduler_runtime_gates.py"
      provides: "Regression for steam elapsed-window contract"
  key_links:
    - from: "scheduler.py"
      to: "model/filtros.py"
      via: "aplicar_triple_gate -> gate5_motivacao"
      pattern: "gate5_motivacao"
    - from: "scheduler.py"
      to: "data/steam_monitor.py"
      via: "calcular_steam -> calcular_bonus_edge_score"
      pattern: "steam_confirmado"
---

<objective>
Implementar durabilidade de contexto para Gate 5 e maturidade temporal para steam antes de bonificação.

Purpose: cumprir GATE-01 e STEAM-01 reduzindo falso-positivo por perda de cache entre reinícios e por steam precoce.
Output: cache persistente com TTL + regra de janela mínima de mercado aberto com regressões dedicadas.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@model/filtros.py
@data/steam_monitor.py

<interfaces>
From model/filtros.py:
- buscar_standings(liga_id, temporada=None)
- gate5_motivacao(time_casa, time_fora, liga_nome)

From data/steam_monitor.py:
- calcular_steam(jogo, mercado, dados_atuais)
- calcular_bonus_edge_score(steam_data)

Current contract notes:
- `calcular_steam` returns dict with `steam_confirmado`, `horas_desde_abertura`, and magnitude metadata.
- `gate5_motivacao` currently depends on in-memory cache dictionaries only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persistir cache de standings com TTL controlado para Gate 5</name>
  <files>model/filtros.py, tests/test_filtros_gate.py</files>
  <behavior>
    - Test 1: Standings validos reaproveitam cache persistido apos reinicio de processo dentro do TTL.
    - Test 2: Cache expirado aciona refresh remoto e sobrescreve snapshot local.
    - Test 3: Falha de leitura/escrita do cache nao quebra o fluxo (fallback para comportamento atual).
  </behavior>
  <action>Adicionar persistencia local simples para cache de standings (com timestamp e chave liga/temporada), preservando fallback resiliente. Implementar invalidação por TTL e manter comportamento nao-bloqueante em erro de IO para evitar travamento do scheduler.</action>
  <verify>
    <automated>python -m unittest tests.test_filtros_gate -v</automated>
  </verify>
  <done>Gate 5 deixa de depender exclusivamente de cache em memoria e reutiliza standings entre ciclos/restarts com TTL valido.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exigir janela minima de mercado aberto para steam bonus</name>
  <files>data/steam_monitor.py, tests/test_scheduler_runtime_gates.py</files>
  <behavior>
    - Test 1: Steam com abertura muito recente nao marca `steam_confirmado` mesmo com magnitude/consenso altos.
    - Test 2: Steam apos janela minima e com consenso/magnitude validos pode confirmar bonus.
    - Test 3: Contrato existente de `calcular_bonus_edge_score` permanece retrocompativel.
  </behavior>
  <action>Introduzir limiar minimo de tempo desde abertura no calculo de steam confirmado (per STEAM-01), preservando regras atuais de magnitude/consenso e mantendo metadata explicita no payload retornado.</action>
  <verify>
    <automated>python -m unittest tests.test_scheduler_runtime_gates -v</automated>
  </verify>
  <done>Bonus de steam passa a exigir maturidade temporal minima, reduzindo ruido de janela curta.</done>
</task>

</tasks>

<verification>
- python -m unittest tests.test_filtros_gate -v
- python -m unittest tests.test_scheduler_runtime_gates -v
</verification>

<success_criteria>
- GATE-01 atendido com cache persistente + TTL validado por regressao.
- STEAM-01 atendido com janela minima de mercado aberto aplicada antes de bonus.
</success_criteria>

<output>
After completion, create `.planning/phases/16-gate-robustness-and-steam-noise-filtering/16-01-SUMMARY.md`
</output>
