---
phase: 16-gate-robustness-and-steam-noise-filtering
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - data/coletar_odds.py
  - model/filtros.py
  - scheduler.py
  - tests/test_filtros_gate.py
  - tests/test_scheduler_quality_prior_ranking.py
  - tests/test_scheduler_provider_health.py
autonomous: true
requirements:
  - GATE-02
  - GATE-03
must_haves:
  truths:
    - "No-vig only runs when opponent odds come from approved sharp-bookmaker source quality"
    - "Divergence gate compares market against base Poisson probability before contextual adjustments"
    - "Scheduler limits same-match concentration in ranking output"
    - "Operator can observe fallback rate when opponent odd is missing in expanded markets"
    - "Minimal drift alert is emitted when quality metrics cross configured degradation threshold"
  artifacts:
    - path: "data/coletar_odds.py"
      provides: "Source-quality metadata and bookmaker whitelist signal for runtime markets"
    - path: "model/filtros.py"
      provides: "Source-aware no-vig validation and base-prob divergence comparison"
    - path: "scheduler.py"
      provides: "Per-match ranking cap, fallback telemetry counters, and minimal drift alert hook"
    - path: "tests/test_filtros_gate.py"
      provides: "Regression for source-quality no-vig and pre-context divergence semantics"
    - path: "tests/test_scheduler_quality_prior_ranking.py"
      provides: "Regression for ranking cap behavior on same-match concentration"
    - path: "tests/test_scheduler_provider_health.py"
      provides: "Regression for fallback/fonte-quality telemetry counters"
  key_links:
    - from: "scheduler.py"
      to: "data/coletar_odds.py"
      via: "formatar_jogos -> odds/source quality payload"
      pattern: "odd_oponente_mercado"
    - from: "scheduler.py"
      to: "model/filtros.py"
      via: "aplicar_triple_gate with base probability and source metadata"
      pattern: "prob_modelo_base"
    - from: "scheduler.py"
      to: "data/clv_brier.py"
      via: "cycle telemetry / drift threshold check"
      pattern: "brier"
---

<objective>
Harden Gate 1 no-vig/divergence semantics with source-quality checks and add immediate portfolio/observability safeguards in runtime selection.

Purpose: cumprir GATE-02 e GATE-03 com comportamento auditavel e reduzir risco imediato de concentração e degradação silenciosa.
Output: no-vig source-aware + divergence pre-context + ranking cap + fallback/drift telemetry minima.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-no-vig-comparison-market-coverage-and-test-baseline/13-01-SUMMARY.md
@.planning/phases/13-no-vig-comparison-market-coverage-and-test-baseline/13-02-SUMMARY.md
@data/coletar_odds.py
@model/filtros.py
@scheduler.py

<interfaces>
From data/coletar_odds.py:
- extrair_melhor_odd(jogo)
- formatar_jogos(dados_api)

From model/filtros.py:
- gate1_ev_e_odd(ev, odd, mercado="", prob_modelo=None, odd_oponente_mercado=None)
- calcular_probabilidade_no_vig(odd_principal, odd_oponente)

From scheduler.py:
- processar_jogos(dry_run=False)
- MARKET_RUNTIME_CONFIG

Current contract notes:
- Gate 1 currently consumes `prob_modelo` directly (contextualized value).
- Runtime ranking currently sorts globally by score and does not cap same-match concentration.
- Provider telemetry already tracks `fallback_used` and can be extended with market-specific counters.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aplicar no-vig apenas com fonte aprovada e divergencia pre-context</name>
  <files>data/coletar_odds.py, model/filtros.py, scheduler.py, tests/test_filtros_gate.py</files>
  <behavior>
    - Test 1: No-vig so e aplicado quando odd principal/oponente vem de qualidade de fonte aprovada (sharp whitelist).
    - Test 2: Sem fonte aprovada ou odd oponente valida, Gate 1 segue fallback legado sem quebrar contrato.
    - Test 3: Divergencia usa probabilidade base de Poisson (pre-ajustes contextuais) no comparativo com mercado.
  </behavior>
  <action>Propagar metadata de qualidade de fonte de odds no pipeline de formatacao e gate payload; restringir no-vig a whitelist explicita de casas sharp; ajustar Gate 1 para comparar mercado contra probabilidade base pre-contexto, preservando reason codes estaveis e fallback compativel.</action>
  <verify>
    <automated>python -m unittest tests.test_filtros_gate -v</automated>
  </verify>
  <done>GATE-02 e GATE-03 operam com semantica source-aware e baseline-prob correta, sem regressao de compatibilidade.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adicionar cap por jogo, telemetria de fallback de mercado e alerta minimo de drift</name>
  <files>scheduler.py, tests/test_scheduler_quality_prior_ranking.py, tests/test_scheduler_provider_health.py</files>
  <behavior>
    - Test 1: Ordenacao final limita quantidade de sinais por jogo apos ranking global.
    - Test 2: Telemetria distingue fallback por ausencia de odd oponente em mercados expandidos (incluindo 1x2_fora/under_2.5).
    - Test 3: Quando metrica de qualidade cruza limiar configurado, evento de drift minimo e emitido no ciclo.
  </behavior>
  <action>Aplicar cap por partida na seleção final para reduzir concentração imediata; enriquecer `provider_health` com contadores de fallback por indisponibilidade de odd oponente e por qualidade de fonte; adicionar verificação leve de drift no ciclo (threshold configurável + log_event/alerta operacional) sem introduzir dependências novas.</action>
  <verify>
    <automated>python -m unittest tests.test_scheduler_quality_prior_ranking tests.test_scheduler_provider_health -v</automated>
  </verify>
  <done>Runtime passa a ter proteção de concentração por jogo e visibilidade operacional de fallback/drift em execução diária.</done>
</task>

</tasks>

<verification>
- python -m unittest tests.test_filtros_gate -v
- python -m unittest tests.test_scheduler_quality_prior_ranking tests.test_scheduler_provider_health -v
- python scripts/run_tests.py
</verification>

<success_criteria>
- GATE-02 atendido com no-vig condicionado por qualidade de fonte (sharp whitelist).
- GATE-03 atendido com divergencia pre-contexto no Gate 1.
- Fase 16 incorpora safeguards imediatos de concentração e observabilidade minima de fallback/drift.
</success_criteria>

<output>
After completion, create `.planning/phases/16-gate-robustness-and-steam-noise-filtering/16-02-SUMMARY.md`
</output>
