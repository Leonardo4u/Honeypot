---
phase: 02-signal-quality-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - model/signal_policy.py
  - model/filtros.py
  - scheduler.py
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
must_haves:
  truths:
    - "Every rejected signal has explicit and stable reject metadata"
    - "Edge/confidence/gate thresholds are centralized and auditable"
  artifacts:
    - path: "model/signal_policy.py"
      provides: "Single source of truth for signal thresholds and reject reason codes"
    - path: "model/filtros.py"
      provides: "Gate pipeline using centralized policy and structured reject output"
    - path: "scheduler.py"
      provides: "Consumption of centralized threshold policy"
  key_links:
    - from: "model/filtros.py"
      to: "model/signal_policy.py"
      via: "threshold and reason imports"
      pattern: "from signal_policy import"
    - from: "scheduler.py"
      to: "model/signal_policy.py"
      via: "minimum score/confidence imports"
      pattern: "from signal_policy import"
---

<objective>
Centralize signal-quality policy and make gate reject reasons explicitly auditable.

Purpose: Eliminate threshold drift and make filter decisions transparent for post-run diagnostics.
Output: One policy module and gate/scheduler integration with stable reject metadata.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-signal-quality-controls/02-RESEARCH.md
@model/filtros.py
@scheduler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create centralized signal policy module</name>
  <files>model/signal_policy.py</files>
  <read_first>model/filtros.py, scheduler.py</read_first>
  <action>Create `model/signal_policy.py` exporting: `MIN_EDGE_SCORE`, `MIN_CONFIANCA`, market EV minimums, odd bounds, and reject reason codes dictionary (`REJECT_CODE_*`). Include a function `get_market_ev_min(mercado)` and a helper `build_reject(reason_code, message, gate_name)` returning structured dict. Keep numeric defaults aligned to current behavior initially (for example odd min 1.55, odd max 3.50, market EV minimum 0.06) so behavior does not regress during refactor.</action>
  <verify>python -m py_compile model/signal_policy.py</verify>
  <acceptance_criteria>
    - model/signal_policy.py contains "MIN_EDGE_SCORE"
    - model/signal_policy.py contains "MIN_CONFIANCA"
    - model/signal_policy.py contains "build_reject(" 
    - model/signal_policy.py contains "REJECT_CODE_"
  </acceptance_criteria>
  <done>Signal policy constants and reject code helpers exist in one module.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor filtros and scheduler to consume centralized policy</name>
  <files>model/filtros.py, scheduler.py</files>
  <read_first>model/signal_policy.py, model/filtros.py, scheduler.py</read_first>
  <action>In `model/filtros.py`, replace hardcoded threshold literals with imports from `signal_policy.py` and ensure blocked return payload includes stable fields: `bloqueado_em`, `motivo`, `reason_code`, and `detalhes`. In `scheduler.py`, replace local constants `MIN_EDGE_SCORE` and `MIN_CONFIANCA` with imports from `signal_policy.py` to keep one threshold source. Preserve current approval semantics while improving audit fields.</action>
  <verify>python -m py_compile model/filtros.py; python -m py_compile scheduler.py</verify>
  <acceptance_criteria>
    - model/filtros.py contains "reason_code"
    - model/filtros.py contains "from signal_policy import"
    - scheduler.py contains "from signal_policy import MIN_EDGE_SCORE, MIN_CONFIANCA"
  </acceptance_criteria>
  <done>Gate and scheduler thresholds are centralized and reject outputs are machine-auditable.</done>
</task>

</tasks>

<verification>
- [ ] python -m py_compile model/signal_policy.py
- [ ] python -m py_compile model/filtros.py
- [ ] python -m py_compile scheduler.py
</verification>

<success_criteria>
- All tasks completed
- Reject reasons are structured and stable
- Threshold values come from one policy module
</success_criteria>

<output>
After completion, create `.planning/phases/02-signal-quality-controls/02-01-SUMMARY.md`
</output>
