---
phase: 02-signal-quality-controls
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - data/kelly_banca.py
  - data/clv_brier.py
  - data/database.py
  - scheduler.py
autonomous: true
requirements:
  - QUAL-03
  - QUAL-04
must_haves:
  truths:
    - "Stake sizing always enforces safety caps even for malformed inputs"
    - "CLV/Brier records remain consistently linked to valid signal IDs"
  artifacts:
    - path: "data/kelly_banca.py"
      provides: "Hardened stake guardrails and malformed-input protection"
    - path: "data/clv_brier.py"
      provides: "Integrity checks before tracking writes and updates"
    - path: "data/database.py"
      provides: "Supportive linkage validation helpers"
  key_links:
    - from: "scheduler.py"
      to: "data/kelly_banca.py"
      via: "safe kelly result handling"
      pattern: "calcular_kelly"
    - from: "data/clv_brier.py"
      to: "data/database.py"
      via: "signal linkage existence checks"
      pattern: "sinal_id"
---

<objective>
Harden stake safety and tracking linkage integrity for post-result quality telemetry.

Purpose: Prevent unsafe stakes and avoid orphan/inconsistent CLV-Brier records.
Output: Defensive stake checks and explicit linkage validation across tracking writes.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-signal-quality-controls/02-RESEARCH.md
@.planning/phases/02-signal-quality-controls/02-01-PLAN.md
@data/kelly_banca.py
@data/clv_brier.py
@data/database.py
@scheduler.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add malformed-input-safe guards to kelly sizing path</name>
  <files>data/kelly_banca.py, scheduler.py</files>
  <read_first>data/kelly_banca.py, scheduler.py</read_first>
  <action>In `data/kelly_banca.py`, add explicit numeric validation for `prob_modelo`, `odd`, `edge_score`, `sinais_abertos`, and `sinais_liga_hoje` before calculations. Clamp/guard impossible ranges (`prob_modelo` outside 0..1, `odd <= 1`, NaN inputs) and return `aprovado=False` with stable `motivo` codes. Ensure final stake percent still respects floor/ceiling constraints and never returns negative or NaN values. In `scheduler.py`, harden caller handling so any invalid kelly response is logged and safely skipped without breaking loop progression.</action>
  <verify>python -m py_compile data/kelly_banca.py; python -m py_compile scheduler.py</verify>
  <acceptance_criteria>
    - data/kelly_banca.py contains explicit validation for prob_modelo and odd
    - data/kelly_banca.py contains non-negative stake guard
    - scheduler.py contains safe skip path for invalid kelly response
  </acceptance_criteria>
  <done>Malformed staking inputs cannot produce unsafe stake outputs or crash processing.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce CLV/Brier signal linkage integrity</name>
  <files>data/clv_brier.py, data/database.py</files>
  <read_first>data/clv_brier.py, data/database.py</read_first>
  <action>Add a small helper in `data/database.py` to verify signal existence by `sinal_id` and expose it for tracking modules. In `data/clv_brier.py`, before inserts/updates, validate `sinal_id` existence and reject writes for missing IDs with explicit log category. Add defensive handling for duplicate closure updates (idempotent updates) so repeated closure attempts do not corrupt metrics.</action>
  <verify>python -m py_compile data/clv_brier.py; python -m py_compile data/database.py</verify>
  <acceptance_criteria>
    - data/database.py contains helper to verify signal ID existence
    - data/clv_brier.py contains pre-write sinal_id existence check
    - data/clv_brier.py contains idempotent/duplicate closure protection path
  </acceptance_criteria>
  <done>Tracking writes only occur for valid signals and repeated updates are safely handled.</done>
</task>

</tasks>

<verification>
- [ ] python -m py_compile data/kelly_banca.py
- [ ] python -m py_compile scheduler.py
- [ ] python -m py_compile data/clv_brier.py
- [ ] python -m py_compile data/database.py
</verification>

<success_criteria>
- All tasks completed
- Stake sizing remains safe under malformed inputs
- CLV/Brier linkage remains consistent and auditable
</success_criteria>

<output>
After completion, create `.planning/phases/02-signal-quality-controls/02-02-SUMMARY.md`
</output>
