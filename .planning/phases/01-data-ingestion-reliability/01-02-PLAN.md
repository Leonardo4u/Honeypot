---
phase: 01-data-ingestion-reliability
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - scheduler.py
  - data/forma_recente.py
  - data/sos_ajuste.py
autonomous: true
requirements:
  - DATA-03
  - DATA-04
must_haves:
  truths:
    - "Malformed xG/form inputs do not crash model analysis loops"
    - "Every run emits provider health summary counters"
  artifacts:
    - path: "scheduler.py"
      provides: "Input-validation gate and health-summary output"
    - path: "data/forma_recente.py"
      provides: "Safe defaults with explicit fallback labeling"
    - path: "data/sos_ajuste.py"
      provides: "Resilient xG/SOS fallback behavior"
  key_links:
    - from: "scheduler.py"
      to: "data/forma_recente.py"
      via: "validated confidence and adjustment calls"
      pattern: "calcular_confianca_dados"
    - from: "scheduler.py"
      to: "data/sos_ajuste.py"
      via: "validated xg source computation"
      pattern: "calcular_xg_com_sos"
---

<objective>
Add pre-analysis input validation and provider health summarization in scheduler runtime.

Purpose: Prevent analysis crashes from malformed data and improve observability of ingestion quality per cycle.
Output: Guard rails in analysis pipeline plus run-level health summary diagnostics.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-ingestion-reliability/01-RESEARCH.md
@.planning/phases/01-data-ingestion-reliability/01-01-PLAN.md
@scheduler.py
@data/forma_recente.py
@data/sos_ajuste.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add strict pre-analysis input validation in scheduler flow</name>
  <files>scheduler.py</files>
  <read_first>scheduler.py, model/analisar_jogo.py, data/coletar_odds.py</read_first>
  <action>Inside `processar_jogos()` before building `dados_analise`, add explicit guards for required fields (`home_team`, `away_team`, `jogo`, `liga`, odds per market, horario). If required value is missing/invalid, skip that market or game with categorized reason (`invalid_input_missing_field`, `invalid_input_odd`) and continue processing others. Add helper block/function in `scheduler.py` to keep guard logic centralized and reusable within the loop. Ensure this path never raises and never interrupts full cycle.</action>
  <verify>python -m py_compile scheduler.py</verify>
  <acceptance_criteria>
    - scheduler.py contains "invalid_input_missing_field"
    - scheduler.py contains "invalid_input_odd"
    - scheduler.py contains a dedicated validation helper used in processar_jogos
  </acceptance_criteria>
  <done>Malformed input is isolated to current item and does not crash or stop batch execution.</done>
</task>

<task type="auto">
  <name>Task 2: Emit provider/data health summary per analysis run</name>
  <files>scheduler.py, data/forma_recente.py, data/sos_ajuste.py</files>
  <read_first>scheduler.py, data/forma_recente.py, data/sos_ajuste.py, .planning/phases/01-data-ingestion-reliability/01-01-PLAN.md</read_first>
  <action>In `scheduler.py`, create per-run counters dict with keys `ok`, `timeout`, `http_error`, `connection_error`, `empty_payload`, `invalid_input`, `fallback_used`; increment counters while processing leagues/games/markets and print a final compact summary line at the end of `processar_jogos()`. In `data/forma_recente.py` and `data/sos_ajuste.py`, ensure fallback paths return explicit fallback markers/labels (not silent defaults) that scheduler can classify into `fallback_used` when source confidence is reduced.</action>
  <verify>python -m py_compile scheduler.py; python -m py_compile data/forma_recente.py; python -m py_compile data/sos_ajuste.py</verify>
  <acceptance_criteria>
    - scheduler.py contains "fallback_used"
    - scheduler.py contains "health" summary print for processar_jogos
    - data/forma_recente.py contains explicit fallback/source marker
    - data/sos_ajuste.py contains explicit fallback/source marker
  </acceptance_criteria>
  <done>Run-level ingestion quality is visible and fallback behavior is explicit and traceable.</done>
</task>

</tasks>

<verification>
- [ ] python -m py_compile scheduler.py
- [ ] python -m py_compile data/forma_recente.py
- [ ] python -m py_compile data/sos_ajuste.py
</verification>

<success_criteria>
- All tasks completed
- Input validation prevents malformed data crashes
- Health summary exposes ingestion outcomes and fallback usage
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-ingestion-reliability/01-02-SUMMARY.md`
</output>
