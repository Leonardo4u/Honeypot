---
phase: 01-data-ingestion-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/ingestion_resilience.py
  - data/coletar_odds.py
  - data/verificar_resultados.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
must_haves:
  truths:
    - "Scheduler can continue when an odds or result provider has transient failures"
    - "Provider failures are categorized (timeout/http/connection/empty)"
  artifacts:
    - path: "data/ingestion_resilience.py"
      provides: "Shared retry and error-classification helper"
    - path: "data/coletar_odds.py"
      provides: "Odds provider calls wrapped with resilience helper"
    - path: "data/verificar_resultados.py"
      provides: "Results provider calls wrapped with resilience helper"
  key_links:
    - from: "data/coletar_odds.py"
      to: "data/ingestion_resilience.py"
      via: "import and retry wrapper"
      pattern: "from ingestion_resilience import"
    - from: "data/verificar_resultados.py"
      to: "data/ingestion_resilience.py"
      via: "import and retry wrapper"
      pattern: "from ingestion_resilience import"
---

<objective>
Implement a shared resilience layer for external data ingestion and wire current provider calls to it.

Purpose: Reduce brittle ingestion behavior and standardize transient-failure handling.
Output: Reusable retry/error-classification helper plus provider integrations for odds and results APIs.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-ingestion-reliability/01-RESEARCH.md
@scheduler.py
@data/coletar_odds.py
@data/verificar_resultados.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared ingestion resilience helper</name>
  <files>data/ingestion_resilience.py</files>
  <read_first>data/coletar_odds.py, data/verificar_resultados.py</read_first>
  <action>Create `data/ingestion_resilience.py` with a reusable function `request_with_retry(url, headers=None, params=None, timeout=10, attempts=3, backoff_seconds=0.8, source_name="provider")` that uses `requests.get` and returns a dict with keys: `ok` (bool), `status` ("ok"|"timeout"|"http_error"|"connection_error"|"empty_payload"|"unknown_error"), `status_code` (int or None), `attempts_used` (int), `data` (parsed JSON or None), `error` (string or None), and `source` (source_name). Retry only on timeout/connection/5xx. Do not retry 4xx except 429. Include bounded backoff `sleep(backoff_seconds * attempt_index)`.</action>
  <verify>python -m py_compile data/ingestion_resilience.py</verify>
  <acceptance_criteria>
    - data/ingestion_resilience.py contains "def request_with_retry("
    - data/ingestion_resilience.py contains "status" and "attempts_used"
    - data/ingestion_resilience.py contains "timeout" and "http_error"
  </acceptance_criteria>
  <done>Shared helper exists and can be imported by provider modules.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate odds and results providers with resilience helper</name>
  <files>data/coletar_odds.py, data/verificar_resultados.py</files>
  <read_first>data/ingestion_resilience.py, data/coletar_odds.py, data/verificar_resultados.py</read_first>
  <action>In `data/coletar_odds.py`, replace direct `requests.get` call inside `buscar_jogos_com_odds` with `request_with_retry` and preserve current function output type (`list` payload on success, `[]` on failure) while logging categorized status in print message. In `data/verificar_resultados.py`, route fixtures request through `request_with_retry` and preserve current output contract (`dict` status payload or `None`) while including categorized failure reason in logs. Do not change public function signatures.</action>
  <verify>python -m py_compile data/coletar_odds.py; python -m py_compile data/verificar_resultados.py</verify>
  <acceptance_criteria>
    - data/coletar_odds.py contains "request_with_retry(" 
    - data/verificar_resultados.py contains "request_with_retry(" 
    - data/coletar_odds.py contains "status" in failure logging path
    - data/verificar_resultados.py contains "status" in failure logging path
  </acceptance_criteria>
  <done>Both provider modules use standardized resilience behavior without changing external API contracts.</done>
</task>

</tasks>

<verification>
- [ ] python -m py_compile data/ingestion_resilience.py
- [ ] python -m py_compile data/coletar_odds.py
- [ ] python -m py_compile data/verificar_resultados.py
</verification>

<success_criteria>
- All tasks completed
- Retry/error classification implemented and reused
- Existing provider functions keep backward-compatible return contracts
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-ingestion-reliability/01-01-SUMMARY.md`
</output>
