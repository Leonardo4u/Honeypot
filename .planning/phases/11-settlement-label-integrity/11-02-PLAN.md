---
phase: 11-settlement-label-integrity
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - scheduler.py
  - tests/test_scheduler_settlement_integrity.py
autonomous: true
requirements:
  - WR-04
must_haves:
  truths:
    - "Pending signals from previous days are still eligible for settlement checks"
    - "Scheduler settlement reuses persisted fixture identity to avoid rematching drift across cycles"
    - "Settlement path remains idempotent and does not overwrite already-finalized outcomes"
  artifacts:
    - path: "scheduler.py"
      provides: "Date-safe pending settlement loop integrated with deterministic resolver"
    - path: "tests/test_scheduler_settlement_integrity.py"
      provides: "Regression tests for cross-day pending settlement and fixture-id reuse"
  key_links:
    - from: "scheduler.py"
      to: "data/verificar_resultados.py"
      via: "resolver invocation with fixture identity and scheduled kickoff context"
      pattern: "buscar_resultado_jogo|fixture_id|horario"
    - from: "scheduler.py"
      to: "data/database.py"
      via: "pending fetch and fixture identity update before final settlement"
      pattern: "status = 'pendente'|atualizar_resultado"
---

<objective>
Integrate deterministic resolver into scheduler so pending settlements are date-safe and robust across days.

Purpose: Complete WR-04 by removing same-day settlement assumptions while preserving operational side effects and idempotency.
Output: Scheduler settlement integration + cross-day regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-settlement-label-integrity/11-01-SUMMARY.md
@scheduler.py
@data/database.py
@data/verificar_resultados.py
@tests/test_scheduler_dry_run.py

<interfaces>
From scheduler.py:
- verificar_resultados_automatico()

From data/database.py (after 11-01):
- atualizar_resultado(sinal_id, resultado, lucro)
- fixture identity persistence helper for `sinal_id`

From data/verificar_resultados.py (after 11-01):
- buscar_resultado_jogo(...) returning `fixture_id`, `fixture_data`, `match_strategy`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace same-day settlement lookup with date-safe pending resolver flow (WR-04)</name>
  <files>scheduler.py, tests/test_scheduler_settlement_integrity.py</files>
  <behavior>
    - Test 1: A pending signal from a previous day is still checked and can be settled when fixture becomes final.
    - Test 2: Scheduler passes persisted `fixture_id_api` when available and avoids name-only rematching.
    - Test 3: If resolver returns non-final status, signal remains pending with no incorrect settlement write.
  </behavior>
  <action>Update settlement loop to consume deterministic resolver context (teams, horario, fixture identity) instead of defaulting to today-only lookup. Ensure this path handles cross-day pendings and preserves existing bankroll/Brier/reaction/Excel side effects only after successful final settlement.</action>
  <verify>
    <automated>python -m unittest tests.test_scheduler_settlement_integrity -v</automated>
  </verify>
  <done>Scheduler settlement no longer relies on same-day assumptions and uses persisted fixture identity whenever available.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Keep idempotency and runtime compatibility in settlement lifecycle</name>
  <files>scheduler.py</files>
  <action>Guard the settlement path so already-finalized signals are not reprocessed and partial resolver outcomes do not trigger inconsistent side effects. Keep existing log style and cycle health behavior compatible with current monitoring patterns.</action>
  <verify>
    <automated>python -m unittest tests.test_scheduler_dry_run -v</automated>
  </verify>
  <done>Settlement integration is operationally stable and does not regress dry-run scheduler flow.</done>
</task>

</tasks>

<verification>
- [x] python -m unittest tests.test_scheduler_settlement_integrity -v
- [x] python -m unittest tests.test_scheduler_dry_run -v
</verification>

<success_criteria>
- WR-04 satisfied: pending bets are settled correctly beyond same-day lookup windows.
- Scheduler uses deterministic resolver outputs and fixture identity from persistence.
- Settlement side effects remain idempotent and backward-compatible.
</success_criteria>

<output>
After completion, create .planning/phases/11-settlement-label-integrity/11-02-SUMMARY.md
</output>
