---
phase: 11-settlement-label-integrity
plan: 01
type: execute
wave: 1
depends_on:
  - 10-02
files_modified:
  - data/database.py
  - data/verificar_resultados.py
  - tests/test_settlement_fixture_resolution.py
  - tests/test_database_integration.py
autonomous: true
requirements:
  - WR-03
must_haves:
  truths:
    - "Settlement matching prefers stable fixture identity when available and does not rely only on team-name substring checks"
    - "When fixture id is missing, resolver chooses one deterministic candidate using kickoff/date-safe logic"
    - "Resolved fixture identity is persisted for future settlement checks"
  artifacts:
    - path: "data/verificar_resultados.py"
      provides: "Fixture resolver with id-first strategy and deterministic date-window fallback"
    - path: "data/database.py"
      provides: "Schema support for fixture identity and persistence helpers"
    - path: "tests/test_settlement_fixture_resolution.py"
      provides: "Regression tests for resolver determinism and fallback ordering"
    - path: "tests/test_database_integration.py"
      provides: "Database migration/integration tests for fixture identity fields"
  key_links:
    - from: "data/verificar_resultados.py"
      to: "https://v3.football.api-sports.io/fixtures"
      via: "request_with_retry with fixture-id and date-window lookup"
      pattern: "request_with_retry\(|fixtures"
    - from: "data/verificar_resultados.py"
      to: "data/database.py"
      via: "returned fixture identity persisted for later scheduler cycles"
      pattern: "fixture_id|fixture_data"
---

<objective>
Build deterministic fixture resolution primitives so settlement labels are bound to the correct match.

Purpose: Complete WR-03 foundation before scheduler wiring by introducing stable fixture identity and deterministic fallback logic.
Output: Resolver contract + DB schema support + focused regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-settlement-label-integrity/11-RESEARCH.md
@data/verificar_resultados.py
@data/database.py
@tests/test_database_integration.py

<interfaces>
From data/verificar_resultados.py:
- buscar_resultado_jogo(time_casa, time_fora, data=None)
- avaliar_mercado(resultado, mercado, odd)

From data/database.py:
- criar_banco()
- garantir_colunas_sinais()
- atualizar_resultado(sinal_id, resultado, lucro)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic fixture resolver with id-first and date-window fallback (WR-03)</name>
  <files>data/verificar_resultados.py, tests/test_settlement_fixture_resolution.py</files>
  <behavior>
    - Test 1: If `fixture_id_api` is provided, resolver queries fixture by id first and uses that result.
    - Test 2: Without fixture id, resolver searches a configurable date window and chooses deterministic candidate by team normalization plus kickoff proximity.
    - Test 3: Ambiguous or missing candidates return explicit non-final status metadata without forcing settlement.
    - Test 4: Returned payload includes fixture identity (`fixture_id`, `fixture_data`, `match_strategy`) for persistence.
  </behavior>
  <action>Refactor result lookup into a resolver path that is deterministic and traceable. Keep API usage on existing provider and preserve `avaliar_mercado` contract. Do not introduce new dependencies or change market evaluation rules in this task.</action>
  <verify>
    <automated>python -m unittest tests.test_settlement_fixture_resolution -v</automated>
  </verify>
  <done>Fixture lookup is deterministic and returns stable identity metadata needed by scheduler settlement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend sinais schema with fixture identity fields and persistence helpers (WR-03)</name>
  <files>data/database.py, tests/test_database_integration.py</files>
  <behavior>
    - Test 1: Fresh DB creation contains fixture identity columns in `sinais`.
    - Test 2: Existing DB migration path adds missing fixture columns idempotently.
    - Test 3: Helper update persists fixture identity for a signal without changing final settlement state.
  </behavior>
  <action>Add nullable fixture identity columns (for example `fixture_id_api`, `fixture_data_api`) through existing schema-guard pattern and create a small helper to persist this identity per `sinal_id`. Keep current insert/update contracts backward-compatible for callers that do not pass fixture metadata.</action>
  <verify>
    <automated>python -m unittest tests.test_database_integration -v</automated>
  </verify>
  <done>Database layer can store and update deterministic fixture identity safely across fresh and existing environments.</done>
</task>

</tasks>

<verification>
- [ ] python -m unittest tests.test_settlement_fixture_resolution -v
- [ ] python -m unittest tests.test_database_integration -v
</verification>

<success_criteria>
- WR-03 foundation is implemented: resolver can anchor settlement to a stable fixture identity.
- Deterministic fallback logic exists when fixture id is absent.
- Fixture identity persistence is available and backward-compatible.
</success_criteria>

<output>
After completion, create .planning/phases/11-settlement-label-integrity/11-01-SUMMARY.md
</output>
